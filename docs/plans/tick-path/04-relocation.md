# Step 4 — relocate tick-reachable ATC logic into `Yaat.Sim`

Part of [tick-path unification](./README.md). The first slice — attendance as a recorded input, then the track-automation bodies (delayed handoffs, auto-accept, point-out auto-ack, the autotrack passes) — is planned in [04a-attendance-and-track-automation.md](./04a-attendance-and-track-automation.md) (approved 2026-09-06; sub-commits A/B/C, predicted-vs-got recorded there as each lands). The checkboxes below are the scope as designed.

- [ ] 4. Relocate tick-reachable ATC logic into `Yaat.Sim`; TDLS and coast as sim core + wire projection; attendance as a recorded input; server session-persistence DTOs collapse into the Sim snapshot (ADR 0003)
  - [x] Attendance as a recorded input — shipped 2026-09-06 with the three attendance-gated bodies and the autotrack passes as Sim steps (sub-commits A/B/C in [04a](./04a-attendance-and-track-automation.md); `SimulationEngine.Attendance`, `RecordedAttendanceChange`, `SimulationEngine.TrackAutomation`). Remaining step-4 host steps: `LiveTrafficSync`, `CoordinationTimers`, `TowerLists` and the strip / broadcast members (the TDLS members crossed 2026-09-07)
  - [x] Step-4 follow-up 1 — shipped 2026-09-07: the resolved ATC roster is snapshotted (`ScenarioSnapshotDto.AtcPositions`) and restored on a Sim-side replay from snapshot 0 (`SessionRecording.InitialAtcPositions`), and a CRC `.AUTOTRACK` is the second recorded input (`RecordedAutoTrackChange` → `SimulationEngine.ApplyAutoTrackChange`, moved verbatim from `RoomEngine`). Predicted twelve baselines byte-identical (every oracle leg loads through the server, no fixture issues `.AUTOTRACK`); got byte-identical, no corpus triage (11,534 / 2,150 green cross-repo). Decision (user): persist the roster rather than resolve `AtcEntries` at replay load, so `InitializeTrackPositions` stays server-side; a pre-feature recording keeps an empty roster on the client by design
  - [x] Step-4 follow-up 2 — shipped 2026-09-07: `SessionPersistenceService.RestoreRoomFromArchiveAsync` runs the whole rebuild under `TrainingRoom.GuardAsync`, the tick loop's own gate; `TrainingRoomManager.RoomRegistered` (outside the lock) is the test seam that takes the gate at registration and proves the restore cannot finish while it is held. yaat-server only; no oracle exposure
  - [x] Step-4 follow-up 3 — shipped 2026-09-07: the point-out auto-ack inverted 7110.65 §5-4-7.a.1.(a); `TickPointoutTimeout` now withdraws a point-out nobody acts on after `SimScenarioState.PointoutNoActionSeconds` (30 s, its own constant — user decision — so an RPO-worked position can still answer) and tells the initiator to coordinate verbally; the solo guard matches the student by TCP or position (it keyed on `StudentTcp` alone). Aviation review before and after; withdraw everywhere, no a.1.(b) carve-out. Predicted twelve baselines byte-identical (no fixture point-outs anyone); got byte-identical, and the corpus did not move (no committed recording has a live auto-ack). Also deleted the unread `Attendance.IsOwnerAttended` / `IsPositionIdAttended`
  - [x] Sim-time, not `DateTime.UtcNow`, for `TdlsItemRecord.CreatedUtc/SentUtc/WilcoUtc/ExpiresUtc` (`TdlsCommandHandler.cs:80,139,142,190`, `TickProcessor.cs:254,265,312`), the strip PDT/ETA text (`StripMutations.cs:907-932`) and the METAR observation clock (`RoomEngine.MetarAnchorUtc`) — every rewound PDC and strip shows the reconstruction's clock today. Design: one pinned instant, `SimScenarioState.SessionStartUtc`, **replaces** `MagneticModelDateUtc` (now derived as its UTC day) and `SimTimeUtc = start + elapsed` is the session's time of day on every run kind (user decisions 2026-09-07: replace rather than add a parallel field; delete the legacy manifest field and rewrite the two fixtures that carried it; the METAR anchor moves in the same step; TDLS keeps `DateTime` fields valued in sim time). Three sub-commits:
    - [x] **A — `SessionStartUtc` replaces the magnetic-model day** (shipped 2026-09-07): the instant comes from the room's `TimeProvider` at a live load *and at a restart* (`RecordingManager.RestartScenarioAsync` passed the process day — found by the pre-plan review, pinned red-first by `Restart_StampsTheAdvancedClock`), the process day for headless/soak/oracle rooms, the recorded value on replay; snapshot schema 20→21; `RecordingManifest` / `SessionRecording` / `RoomCheckpointManifest` carry `SessionStartUtc` with the same `RecordedAtUtc.Date` / `SavedAtUtc.Date` fallbacks; the two fixtures whose manifest carried `MagneticModelDateUtc` (`parallel-mlt-from-upwind-recording…zip`, `s2-oak5-follow-heli-recording.zip`, both 2026-09-03) had their manifests rewritten. Predicted twelve oracle baselines byte-identical (every leg loads through `HeadlessRoom` with the process day); got byte-identical, no corpus triage (11,709 / 2,157 green cross-repo). Corrections to the brief: 21 Sim test call sites passed `magneticModelDateUtc:` by name and were renamed; no test class is named `S2Oak5FollowHeli*` — the heli fixture is loaded by three other classes, all green. Review catches (csharp-reviewer): the v1→v2 `MigrateToV2` path dropped the start on both ends (now carried, pinned over `sample-recording.json`); the rewind pin could not fail without advancing the harness clock; the clock's no-clock fallback is the unclamped `SimScenarioState.ProcessDayUtc`, not the WMM-clamped `EvaluationDateUtc`; the recording upgrader's room runs on `TimeProvider.System` instead of a 1970 manual clock
    - [x] **B — TDLS timestamps and strip PDT/ETA on `SimTimeUtc`** (shipped 2026-09-07, yaat-server only): the seven `DateTime.UtcNow` writers read `room.ActiveScenario!.SimTimeUtc` (`TryQueueAutoTdlsForAircraft` takes the instant as a required parameter); `FormatEta`/`BuildArrivalStripFields`/`RequestArrivalStripForAircraft` take the scenario; a departure strip's PDT is the session clock at print (it was real now + elapsed). Red-first held on every pin the review asked for: the absolute `"1202"` PDT and the session-hour ETA (`StripReconstructionClockTests`, both reconstruct-equals-live afterwards), TTL expiry at t=0 (a start+2 h instant is in the real past, so the wall-clock compare expired it immediately), auto-WILCO after three ticks, and the three handler timestamps. Predicted twelve baselines byte-identical (strips/TDLS are outside `CaptureSnapshot`); got byte-identical (11,709 / 2,161 green cross-repo). Corrections to the brief: `RequestFlightStripForAircraft` refuses without a resolved student position, so the pins run over `autotrack-departure.json` / `vfr-arrival-autotrack-no-handoff.json` (ZOA + student), not the `minimal-*` resources; the arrival ETA is bounded to the session hour (its minutes come from `ResolveStripRequest`'s geometry); the 7,200-tick TTL walk sets `ElapsedSeconds` directly; `TtlExpiry_PurgesScheduledWilcoEntry` schedules the entry past the TTL so only the expiry sweep can purge it; the TDLS suites load `empty-oak.json` (a new resource) via `CreateWithOakData` so their hand-seeded `AAL100` does not collide
    - [x] **C — the METAR issuer anchors to `SessionStartUtc`** (shipped 2026-09-07): `MetarAnchorUtc` deleted; `MetarIssuer(weather, sessionStartUtc, elapsedSeconds, locator)` seeds its last-routine instant and every station's baseline from start + elapsed, so the every-second `EnsureLiveMetarIssuer` rebuild after a rewind re-issues nothing on its first tick (red-first: the routine re-stamp on `Tick(3600)`, and the server pin that the :53 routine follows the session clock rather than the room clock advanced 80 min after load). Review catches (csharp-reviewer): the rebuild reverted `Reports` to the load-time text until the next :53 (pre-existing, now deterministic) and a rebuild landing exactly on a :53 skipped that hour — the constructor now composes the routine that fell inside the session; a gusty-profile 'no SPECI from the construction baseline' test was vacuous (bounded ±30° variability never opens the 45° gate) and was deleted; the docs' 'a replay issues at the same sim second' overclaimed (a reconstruction host issues nothing — the true claim is that the :53 grid is a function of sim time). Predicted twelve baselines byte-identical (no oracle room builds an issuer; the issuer is not snapshot state); got byte-identical. The whole item is closed; `IHostSteps`'s step-4 list is unchanged (`TdlsExpiry` stays a host step until `TdlsState` crosses)
  - [x] `FlightStripState` / `TdlsState` into the snapshot, retiring `RoomStateSnapshotMapper`'s duplicate — **shipped 2026-09-07** in three sub-commits (the plan file `04b-strips-and-tdls-snapshot.md` is deleted; the design decisions: engine-owned like `Attendance`, captured in the `Server` section rather than `ScenarioSnapshotDto`, `TrainingRoom.StripState`/`.TdlsState` deleted rather than forwarded, `ScheduledWilcoAt` snapshotted — user decision — and the Sim `TdlsItemStatus`/`TdlsClearance` core with the wire `TdlsStatus`/`ClearanceDto` left server-side per ADR 0003)
    - [x] **A1 — the state crosses, engine-owned, snapshotted; the persistence duplicate collapses** (shipped 2026-09-07): `SimulationEngine.Strips` / `.Tdls` (`Simulation/Strips/FlightStripState.cs`, `Simulation/Tdls/{TdlsState,TdlsClearance}.cs`; `TdlsItemRecord` re-modelled on the Sim `TdlsItemStatus` / `TdlsClearance`, the wire `TdlsStatus` / `ClearanceDto` stay server-side and `DtoConverter` projects), `ServerSnapshotDto.Strips` / `.Tdls` (schema 22, null → empty; `ScheduledWilcoAt` snapshotted — user decision), `TrainingRoom.StripState` / `.TdlsState` deleted (every site reads `room.ActiveSim!.Strips` / `.Tdls`; the no-scenario readers send an empty DTO), `RoomStateSnapshotDto` / `RoomStateSnapshotMapper` lose their strip/TDLS halves, `RecordingManager.ResyncStripsAndTdlsAsync` re-pushes both after a rewind or restart. Red-first held on `StripTdlsRewindTests` ×4 (a strip printed at t=5 survived `RewindAsync(3)`; the abandoned run's Sent PDC survived; a manual strip survived a restart; no full-state push after a rewind). **Oracle — predicted vs got, a prediction miss attributed before rebaselining:** predicted `TdlsTrackRemoval` and `ADD @NEW1` spawn-hook entries on the test/replay legs and NEW1 entries on the reconstruct leg; got neither — no fixture departure acquires a track owner inside its window, and `ADD V S P @NEW1` is a parking spawn, which files no departure airport (only the IFR runway form of `ADD` does), so `IsDepartureAircraft` is false and the spawn hook is never reached through the `ADD` arm on any leg. Got instead, same family (Added-only, host bodies the bare engine and the Sim replay host never run): `s2oak4` live-vs-test / live-vs-replay 8 paths from t=271 (`AfterAircraftSpawned` on N342T's delayed spawn: its Ground-bay strip and PDC) and `actions` 7 paths from t=50 (`ReprintDepartureStripAfterAmendment` on the scripted `FP`); `weather`, `autodelete` and all four reconstruct legs byte-identical. Rebaselined with the body named in every Note (11,727 / 2,167 green cross-repo). Corrections to the plan: **a live room never holds snapshots** (`LoadedSnapshots` / `SnapshotProvider` are import-only), so every live rewind is a from-scratch reconstruction — the plan's "rewind restores the nearest snapshot" only holds for an imported recording; `RecordingManager` has four construction sites, not three; `CrcClientState.Strips.cs`'s three strip-verb handlers and `CrcBroadcastService.BuildFlightStripsData` are no-scenario paths too; `StripRequestRecordingTests.ARewindAcrossTwoRequests_…` pinned the retired "rewind does not reset strips" premise and was rewritten to the new invariant; the canonical `TDLSS` field order (7 = DepFreq, 8 = LocalInfo) is the inverse of the wire DTO's — `TdlsClearance` became init-only properties on review so no positional order exists to get wrong, pinned by `ClearancePayloadFromFields_MapsFieldSevenToDepFreqAndFieldEightToLocalInfo`; the plan's A1 server test 2 ("Sent with payload, WILCO at the live second") contradicted its own item 10 (the TDLS replay refusal kept in A1) and became `Rewind_DropsTheAbandonedRunsSentPdc` — the Sent-with-payload pin is A2's. Review catches (csharp-reviewer): the null-section restore called `Strips.Reset()` and wiped the ARTCC rack skeleton the reload had just built (now `ClearSession`, mirroring TDLS); the post-rewind resync was fire-and-forget (now awaited in order); `StripCommandHandler` re-read `room.ActiveSim!` inside the lock it had just taken on it. **Debt this made visible, retired in C below:** `SEP` / `HSC` / `SCAN` ids were Guid-minted at dispatch and not baked onto their records (printed strips were, in 3d-5b; `BLANK` draws from a counter). **Consequence for A2:** it had no A1-banked reconstruct entries to retire, so its oracle proof came from extending the `actions` script
    - [x] **A2 — reconstruction fidelity** (shipped 2026-09-07, yaat-server + one Sim doc): `RoomHost.ApplyTdls` / `ApplyTdlsOpsConfig` apply on every run kind (`TdlsNotReconstructed` deleted — this supersedes ADR 0007's "TDLS … the slots refuse while replaying" bullet: the verbs were always recorded, only refused on replay); `RoomHost.OnAircraftSpawned` runs `TickProcessor.AfterAircraftSpawned` on every run kind and only `RoomEngine.BroadcastAircraftSpawned` is live-only (`AnnounceAircraftSpawned` deleted); `ApplyRecordedStripRequest` broadcasts unless `IsBroadcastSuppressed`, so tape playback pushes each print. **Oracle, red-first through the fixture:** the `actions` script gained `TDLSQ`@56 + `TDLSS`@58 on the scenario departure N172SP and `ADD I L J 28R`@110 (option C — no `ADD` form fixes a callsign, and only the IFR runway form files a departure airport; a parking spawn files none, which is the true reason `ADD V S P @NEW1` reaches no hook). Predicted before the change: live-vs-reconstruct `Added` on the new aircraft's strip/PDC paths and on N172SP's PDC `Status`/`SentUtc`/`SentPayload`/`ScheduledWilco`; got exactly that — 16 paths from t=58, plus the same 8-path family on live-vs-test / live-vs-replay, nothing `Removed`, three fixtures byte-identical. After the change: live-vs-reconstruct byte-identical again (all 16 gone), the 8-path family banked on test/replay with Notes naming `ApplyTdls` (refused on the bare/replay hosts) and `AfterAircraftSpawned` (no hooks there) — retires when the bodies cross. Red-first on `StripTdlsRewindTests` ×3 (reconstruction lacks an `ADD`ed aircraft's strip and PDC; a rewind drops the recorded `TDLSS`; playback pushes no `StripItemsChanged`). 11,728 / 2,169 green cross-repo. Corrections: the A2 rewind test's aircraft must come from a recorded `ADD` (a hand-added aircraft is in no action and dies at the router's aircraft-exists guard on replay); `COMMANDS.md` §ADD now says which forms file a departure airport. Review catches (csharp-reviewer): the hub spawn broadcast is gated on `IsBroadcastSuppressed` like the pre-physics spawn path (playback announces, broadcast-then-hooks so a client knows the callsign before its strip); `OnAircraftDeleted` split the same way — the room assignment drops on every run kind (`RoomEngine.SeekLiveTraffic` relied on the bundled tail and now removes it itself), the hub/CRC deletes stay live-only, pinned by `Reconstruction_DropsTheAssignmentOfADeletedAircraft`; `TDLSW`@59 scripted so the recorded verb, not the t=61 timer, moves N172SP to Wilco. **Oracle footgun found on the way:** the assertion keys on the path set and the fixture-level first divergent second, not on each entry's `FirstSecond`, so a script change that only moves *when* a banked path starts diverging passes silently — rebaseline deliberately and check the per-entry seconds by hand. One review item was unreachable and reverted: `OnLiveTrafficHidden`'s suppression add cannot run on replay (the recorded removal empties the world before the `DEL` text re-applies), so the shadow un-suppression after a rewind went to the MAIN.md backlog as a recorded-removal-consumer fix
    - [x] **B — #424** (shipped 2026-09-07): `RecordingManager.RestartScenarioAsync` collects the controller's separators by walking the bays (the record's `Rack`/`Index` are stale-by-design) before the engine is dropped and `StripMutations.RestoreSeparator` appends them, id and label intact, to the fresh racks inside the suppressed block so the existing resync carries them (a bay or rack the reloaded config lacks is logged and dropped); the client's `MainViewModel.ClearScenarioState` empties every Strips entry (primary and split secondary) and every TDLS entry (`VTdlsViewModel.Clear`, shared with its connection-lost path), so Leave Room clears the docked tabs and the popped-out windows that share the VMs. Red-first: the separator vanished on restart; the strips/TDLS views kept the old room's items after `ClearRoomState`. Oracle byte-identical (restart is no oracle path). 11,731 / 2,171 green cross-repo. Facts: separator ids are `SEP_{8 hex}` from `MintShortId`, not `NextBlankId`; review moved the restore to the walk index (a divider under a group of strips comes back under them) and found that the carried separators had no origin in the fresh tape — the first rewind after a restart lost them (a live room has no snapshots) — which became C
    - [x] **C — Guid-minted strip ids are baked; a restart re-issues its separators as recorded `SEP`s** (shipped 2026-09-07, user decision over "log it" / "drop the carry-over"): `BakedDraws.StripId` / `RecordedCommand.StripId` / `ArmContext.StripId` ride the strip arm's new `StripApplyResult`, `IActionHost.ApplyStrip(callsign, command, identity, bakedStripId)` answers the id the host minted or reused, and `StripCommandHandler` honours a baked id for `SEP` / `HSC` / `SCAN` (already present → success, nothing created — the `PrintRequestedStrip` rule; review added `BLANK` to the baked set: its counter id is deterministic but a record re-applied over a t=0 snapshot restore would mint `BLANK_{n+1}` beside the snapshot's `BLANK_{n}`). `RestartScenarioAsync(connectionId, initials)` (the hub threads the caller's) collects the separators as `SEP` terms — style, `FACILITY/Bay` from the accessible-bay set the handler resolves against, 1-based rack/slot from the rack walk, label — and re-issues each through `RoomEngine.IssueLive` at t=0 of the fresh tape, so the ids are fresh mints and every later rewind or export rebuilds them; `RestoreSeparator` / `RestoreSeparators` deleted. Red-first on five server pins (`StripIdBakingTests`: a reconstructed `SEP`+`SEPD <id>`, `HSC`+`HSD <id>`, `SCAN` id, a same-room rewind past a `SEP`, restart-then-rewind — `Expected: 2 Actual: 0`). **Oracle: the fixture cannot see this fix by construction** — `SweepAsync` records the log in one live pass and compares a second live pass, and a Guid is not a seeded draw, so a scripted `SEP` diverged on `Items[*].Id` before *and* after the change (7 paths at t=70, and the set varied 6/7 between runs as dictionary slots collided); the steps were removed, the blind spot is named in the fixture doc and `tick-loop.md`, and the twelve baselines are byte-identical. 11,735 / 2,178 green cross-repo. Corrections: the `SEP` position grammar is 1-based (`rack/index`, `ResolveStripDest` rejects 0); `TickProcessor.DispatchDeferredStripAsync` is a second `Handle` caller (passes no baked id); `RoomEngine.IssueLive` went internal for the restart; a fourth mint site the fix does not reach — a deferred/preset `SCAN`/`HSC` re-derived from `PendingStripDispatches` — is in the MAIN.md backlog
  - [ ] Shrink `IActionHost` as each host-slot body (strips, TDLS, coordination, ASDE-X/SAID, bookmarks) crosses — the action path's step-4 debt, same shape as `IHostSteps`. Plan: [04c-strips-tdls-bodies.md](./04c-strips-tdls-bodies.md)
    - [x] **A — the TDLS bodies cross** (shipped 2026-09-07): `TdlsMutations`, `TdlsCommandHandler` (static, over the engine), the four tick steps (`SimulationEngine.TickAutoTdlsQueue` / `TickTdlsAutoWilco` / `TickTdlsExpiry` / `TickTdlsTrackRemoval`), `TDLSOPS` and the spawn-time auto-queue (`SimulationEngine.AfterAircraftSpawned`, run before the host's spawn consumer at every spawn site — the pre-physics loop, the router's spawn arms and records, `PopulateRoom`) moved verbatim; `IActionHost` lost `ApplyTdls` / `ApplyTdlsOpsConfig`, `IHostSteps` lost four members; the broadcasts became an engine-owned change tracker (`TdlsState.Changes` → `TdlsChangeSet`) drained by `ActionRouter.Finish` and a new `StripTdlsChanges` post-physics step into `OnTdlsChanged` (declared on `ITdlsChangeConsumer`, extended by both host views — the one structural choice the brief had not spelled out, since a `SpineStep.Sim` sees only `IHostConsumers`); `RoomHost` → `TdlsBroadcaster.BroadcastChanges` (items, removals, full state; nothing while suppressed); `SimulationEngine.InitializeStripsAndTdlsFromArtcc` replaces the lifecycle service's two private bodies and the Sim `ReplayDriver` calls it, so a client-side replay builds the same DCL/PDC lists (it refused every TDLS verb before). Red-first: `TdlsStepTests` ×10 was a compile red before the move; mutation: the re-send guard removed → its pin red; the `Dumped` guard is checked twice (helper and `QueuePending`, pre-existing) so only removing both reddens the lockout pin. **Oracle — predicted and got, clause for clause, nothing `Added`:** `actions` live-vs-test / live-vs-replay lost the seven `Server.Tdls.*` paths (58 / 59 / 110), `s2oak4` live-vs-test / live-vs-replay the two at 271; the `Server.Strips.*` entries stay for B; the other eight byte-identical (90 deletions, 0 insertions across four baselines). 11,743 / 2,179 green cross-repo. Corrections to the brief: `TdlsCommandHandler.Handle` / `HandleWilco` / `FormatPilotPdcMessage` and the four `Tick*` are `public` (the server suites drive the single step and `InternalsVisibleTo` does not reach `Yaat.Server.Tests`); `ResolveFacility` resolves the aircraft by exact callsign (`SimulationEngine.FindAircraft`; the server used the FLID-aware `RoomEngine.FindAircraft`, but the router had already resolved by callsign); the spawn hook's log line has no room id; the `[TDLS PDC sent at …]` line now drains after the command echo instead of racing it. Findings on record: `docs/vtdls.md` claimed the auto-WILCO sets `Voice.TdlsDumped` and that a PDC applies a clearance to the pilot — neither is in the code (the flag's only writer is CRC's `TdlsDump` handler, and nothing in Yaat.Sim reads it); the doc now says so. `TdlsCommandHandler.ResolveSid` reads `cfg.Sids`, which is empty at OAK/SFO/BOS (every SID lives in an ops config), so the ACARS text never names a SID there — pre-existing, on the backlog. Review catches (csharp-reviewer): a Sim replay queued a load-time departure's PDC one second late — the Sim `LoadScenario` runs no spawn hook and the ARTCC init follows it — so `ReplayDriver.RangeCore` now runs the hook over the world right after its t=0 `ClearSession` (pinned: `CreatedUtc == SessionStartUtc`; oracle byte-identical, every oracle room loads through `PopulateRoom`); nothing proved the `StripTdlsChanges` spine step delivered (every `OnTdlsChanged` assertion came from the router's `Finish` drain) — `SpineCapturingHost` now drives `RunSecond` and the TTL / track-removal / auto-WILCO pins assert the change set from the tick, and deleting the spine entry reddens all three; the tracker's comment claimed a lock it does not hold (the serialiser is the room's tick gate); `HandleWilco` and the two per-item broadcasters went private. Corpus triage (ADR 0004, over-broad): `StripTdlsEngineSnapshotTests.AReplayFromSecondZero_…` asserted an empty TDLS list after a t=0 replay — the load-time PDC is exactly what a replay must now rebuild — and pins the sharper invariant instead (one fresh Pending `TDLS_1` at the session start, nothing inherited). Ordering note the 'verbatim' framing hides: the pre-physics spawn hook now runs right after `ProcessGenerators`, before `ApplyArrivalSpacing` / triggers / timed presets (the server ran it after the whole pre-physics segment), so a same-second trigger amending a just-spawned aircraft's departure lands after the PDC decision — benign (the per-second sweep catches up; no corpus moved). For B: `PopulateRoom`'s end-of-load drain reads the tracker directly rather than through `DrainStripTdlsChangesInto`, so it must drain strips too when `Strips.Changes` lands
  - [ ] Reconstruct-vs-live-log pin on the S2-OAK-4 bundle of 2026-09-06 (`tests/Yaat.Sim.Tests/TestData/parallel-mlt-from-upwind-recording.yaat-bug-report-bundle.zip`, N342T): the live server went around "traffic on the runway" at t≈1100 and its `[Command] N342T (Upwind): MLT 28L` at t=1125 landed in `UpwindPhase`; the bundle's reconstruction is still `FinalApproach` at the t=1105 snapshot and `GoAround` from 1110, so the recorded MLT hit the `GoAroundPhase` branch. Pin it as `FlightPlanCommandReconstructionKeepsPhaseTests` is pinned: reconstruct to t=1101 → `GoAroundPhase`, t=1125 → `UpwindPhase`. The bundle's server predates 3d-4b (its log still uses the pre-`594a4ac5` `({Phase})` command format), and 3d-0 had already found "reconstruct: reaction delay ignored, 4 s early", so green on main means 3d-4b covered it and the pin banks that; red is a new 3d-class defect to attribute — first suspects the PAUSE/UNPAUSE at t≈1077–1100 (the go-around fired the second the room resumed) and `OccupiedRunwayGoAround.TryTrigger` against N152SP's reaction-delayed `CTO` (t=1076). Either way the oracle's fixtures reach none of this (no fixture pauses mid-run or trips the occupied-runway go-around) — a pause-mid-run fixture belongs in step 5's corpus sweep
