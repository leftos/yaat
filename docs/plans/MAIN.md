# YAAT plans — index

Entry point for `docs/plans/`. One line per item; the detail lives in the linked subplan. Finished plans are deleted, not archived — git history is the record (a plan whose rationale or status table still has reference value is promoted into `docs/` instead). Issue-specific plans live in [`open-issues/`](./open-issues/) and are deleted once implemented.

**Order of work (steer 2026-09-07):** bug reports and feature requests — usually open GitHub issues — come first and ship in hotfix releases. The programmes below them run in the background and release with a non-hotfix. A fresh agent starts at **Bug reports and feature requests**, then the **Current programme**; **Backlog** is unscheduled findings with no report behind them. Folding the open tracker into this file is the `triage-open-issues` skill.

## Bug reports and feature requests

- [ ] Live-session assume UX (ZOA Discord 2026-08-31): bulk-assume modes, snapshot-then-assume-all, snapshot-as-scenario authoring — yaat-server [live-traffic-swim/09-live-sessions.md](../../../yaat-server/docs/plans/live-traffic-swim/09-live-sessions.md) §3. Auto-assume on first command + `UNASSUME` shipped 2026-09-08 (user pick); the right-click menus still offer only Assume control for a shadow and nothing for `UNASSUME` (the client cannot tell an assumed aircraft from a scenario one — the marker is not on the wire), — noted in §3 (the rollback of an auto-assume the seeded state refuses shipped 2026-09-09)

## Current programme

- [ ] **Tick-path unification** — [tick-path/README.md](./tick-path/README.md): one tick spine, one action router, one body per behaviour across live, replay and reconstruction. Steps 1–3 and 3d shipped (ADRs 0001–0007); step 4 (relocation) is underway — attendance as a recorded input and the track-automation bodies shipped 2026-09-06, the session clock and the strips + TDLS snapshot shipped 2026-09-07, and **shrinking `IActionHost` is underway ([tick-path/04-relocation.md](./tick-path/04-relocation.md), the `IActionHost` item): the TDLS (A), strip (B), coordination (C) and tower-list (D) bodies crossed 2026-09-07 — the coordination verbs, the release-expiry timers, the removal on `TRACK` and the P-list step are engine bodies, a coordination item's id is deterministic, the tower-list dwell entries are snapshotted (schema 23), `IHostSteps`'s step-4 debt is `LiveTrafficSync` alone, and all twelve oracle baselines are empty with the `actions` fixture now scripting coordination; the bookmark, session-clock, CRR-group and ASDE-X/SAID-mutation slots crossed 2026-09-08 (`IActionHost` has one slot left); next is the safety-logic configuration (a design call for the user: opaque `ConfigJson` on the engine vs a Sim-native `AsdexSafetyLogicConfig` projected by `DtoConverter`, sequenced with the `ProcessAsdexAlerts` host step it feeds — the alert set and `LiveTrafficSync` are the two `IHostSteps` bodies left, and `LiveTrafficSync` needs its own subplan); plus a reconstruct-vs-live-log pin on the 2026-09-06 S2-OAK-4 bundle (the reconstruction went around 5–10 s after the live run; possibly already fixed by 3d-4b)**. Controller AI waits on this (steer 2026-09-02).

## Programmes next up (background; release with a non-hotfix)

- [ ] **Controller AI v1** — on hold behind the tick work; [controller-ai/README.md](./controller-ai/README.md) (H0 / CA0 / CA1 / K1-lite shipped; the open follow-ups are listed there) and the v1 slice [controller-ai/12-milestone-v1-scope.md](./controller-ai/12-milestone-v1-scope.md). The per-frequency radio model ([controller-ai/11-radio-model.md](./controller-ai/11-radio-model.md)) ships first.
- [ ] Pilot AI for solo training — [pilot-ai-self-training/README.md](./pilot-ai-self-training/README.md) (M10.x and Wave 1 shipped; next is Wave 2, M11.2 pilot-initiated requests)
- [ ] Live traffic via SWIM (#150) — [open-issues/150-live-traffic-swim.md](./open-issues/150-live-traffic-swim.md) → yaat-server `docs/plans/live-traffic-swim/08-remaining-work.md`. **In internal testing** (2026-09-07): the FAA ADX / LADD access is granted and the feed is deployed to YAAT1 with the feature flag off; the flag flips when internal testing finishes
- [ ] **Typed command arguments** (steer 2026-09-07) — [typed-command-arguments.md](./typed-command-arguments.md): the registry names each argument's type instead of a prose hint, one small validator per type, arguments bind to record fields by type, and a slot that takes a runway or an altitude is two overloads resolved like a compiler resolves them — the token is tried against each viable overload's declared type (`RunwayArgument`: 1–2 digits + optional L/C/R; `AltitudeArgument`: 3+ digits) — with no combined type; a runway the airport lacks is rejected at dispatch. Step 1 (the pattern modifiers) is in flight; the audit of the remaining slots is step 2

## Backlog

Findings and small items with no subplan and no report behind them:

- [ ] **Ground-speed realism follow-ups** (standstill-acceleration work, 2026-09-09; design in `docs/flight-physics.md` / `docs/ground/navigator.md`): (a) pushback integrates at the taxi rates (1.0 kt/s up, 5 kt/s down) — a tug is towbar-limited to ~0.3 kt/s / ~1 kt/s brake, ~10–20 ft of position error per push; (b) `FollowingPhase.StopDistanceNm` (~90 ft) is closer than a crew sits behind a stopped widebody (AIM 4-3-18.e jet blast) — scale by the lead's weight class; (c) a brakes-held static takeoff (thrust set before brake release; fighters) is not expressible — `GroundRollProfile.For` takes the idle rate and spool from the category, so F15/F16/F18/F22 ramp 1.0 → 10 kt/s like a rolling takeoff; (d) a touch-and-go leader's projected ground run (`PrecedingDepartureBlock`, `RejectedTakeoff.CanOverfly`) integrates the ramp from a standing start and understates the run by base speed × t — conservative for a leader, so left; (e) the rolling takeoff seeds its spool clock from the speed it arrives with; seeding from the line-up's rollout elapsed time would be more faithful
- [ ] SFO taxiway C in the GeoJSON is not straight: ~7° kinks at edge-split nodes with ~38 ft legs make the navigator's turn-rate feasibility cap saw-tooth the speed (12 → 15 → 15.5 → 17.5 → 25 → 20.9 → 30 kt) and brake 2 kt/s on a run with no real corner (`StandstillAccelerationProfileTests` taxi table) — look at the fillet generator / feasibility cap for near-collinear split nodes
- [ ] `TaxiBudgetDeriver.Derive` plans the route with a hard-coded `AircraftCategory.Jet` regardless of the pair's category (the overheads are per-category since 2026-09-09; the route is not)
- [ ] In the (now deleted, desynced) `CtoMrdPatternExitTests` recording, after the recorded `CTO MRD` binds to runway 15 N784ME sits in `LineUpPhase` stationary from t=553 to t=1000 without reaching `TakeoffPhase` — a stuck-line-up smell worth its own repro
- [ ] **Preferred taxi routes should drive `TAXIAUTO`** (#426 airport sidecar ideas; steer 2026-09-09): when a sidecar lists a preferred taxi route for a destination runway (e.g. OAK 28R: `D C B`), an aircraft that can execute the whole route from its present position, or join it part-way (`C B`), should prefer it over the naive layout traversal — with an implicit preamble (ramp / non-movement taxilane) to reach the first listed taxiway. Also in #426: strip push points in preferred routes, gate operator/type/weight and pushback data, hand-drawn curved taxiways replacing generated ones, ramp-vs-ground frequency areas
- [ ] **A rewind whose nearest snapshot lies after a `DEL` on a shadow still un-suppresses it** (implementer observation 2026-09-08, DEL-shadow fix): `RecordingManager.RewindAsync` restores from `FindNearestSnapshot` and reconstructs only from that second on, so the `RecordedLiveTrafficRemoval` before the snapshot is never replayed and `RoomLiveTrafficState.Suppressed` (room state, not snapshotted) stays empty — needs the suppression set in the room checkpoint/snapshot or a carry-over across the reload, the same shape as the bookmarks carry-over
- [ ] `RecordingManager.RewindAsync` and `RewindFromSnapshotAsync` are ~90 % identical bodies (csharp review 2026-09-09; the resync omission was exactly this duplication): extract `ExecuteRewindAsync(RewindPlan)` with a required-member `RewindPlan` (snapshot, from-second, target, terminal line) and settle deliberately whether `PlaybackEndSeconds` is set before or after the reconstruction (the two differ today; inert until the reconstruction reads it)
- [ ] `LoadRecordingArchiveAsync` / `LoadRecordingAsync` tear down and rebuild the engine under suppression but never run `ResyncEngineStateAsync` (csharp review 2026-09-09): an instructor with printed strips and PDCs who loads a bundle keeps the abandoned run's strips on every client until something ticks; `LoadRecordingAsync` has a comment justifying it, the archive path has none — route both through the resync or document why not
- [ ] `CrcClientState.ReadSharedStateWire` sits at cyclomatic ~26 (lifted verbatim 2026-09-09; the ≤8 cap): two static local helpers over `ref MessagePackReader` (`ReadOptionalString`, `ReadEnumOrInt<T>`) collapse keys 0–3, 6 and 9
- [ ] Every flight-plan amendment now re-broadcasts the aircraft's vTDLS item (2026-09-08), including the high-frequency `CrcImpliedBareSlew` owner-slew beacon accept and empty amendments (the revision counter bumps on a no-op by design): decide whether the mark should skip amendments that changed no header field, or whether the per-item push is cheap enough to leave
- [ ] `DispatchContext` has 15 positional record parameters (csharp review 2026-09-08; the ≤5 cap): every new field touches six engine construction sites — move to init-only properties with object-initializer construction so the next field is a one-line change
- [ ] **A test almanac — `docs/test-map.md`** (steer 2026-09-08): 1,121 test files and 8,099 test methods (920 files / 6,789 methods in `Yaat.Sim.Tests` alone) with no index, so there is no way to answer “is this already covered, and where does a new test belong?” without grepping. Evidence from the queued-dimension work: the full suite stayed green across 62 behaviour changes, and the coverage gap surfaced only because a reviewer said the two-dispatch supersede shape was pinned for the pattern family alone. Map *classes of test* (shape → where they live → what they pin → the fixture/harness they need), keyed by subsystem like `architecture.md`'s Task Index — not a per-file listing, which rots on contact and is derivable. Link it from `docs/README.md` and `docs/test-harness.md`
- [ ] **The command queue has one cursor for independent chains** (implementer finding 2026-09-09, takeoff-dims): a block a fresh compound preserves and the compound's own chain share one list, so whichever is first can hold the other — preserved-first (shipped) lets a pre-armed `AT 1000 CM 5000` fire but makes `TAXI D; SQ 1234` wait behind an unmet `AT 5000 CM 7000` until 5,000 ft. Scope "hold behind an unfired predecessor" to blocks of the same dispatch (`CommandBlock.SourceCommandText` is already the per-dispatch key `DiscardChainRemainder` groups by) at the three advancement sites: `ApplyReadyConditionalBlocks` (record the blocker's text and `continue` instead of `break`), `AdvanceQueueWhileIdle`'s `frontierBroken`, and the regime-A cursor in `UpdateCommandQueue` (a per-chain frontier rather than one cursor — the deep one); pin both counter-cases
- [ ] **A `FiredDimension` registry column** (csharp review 2026-09-08): `GetCommandDimension` is a thirteen-arm predicate ladder (cyclomatic ~20 against the ≤8 limit) and every new verb outside a family predicate has fallen to `None` and wiped the queue (RWY, TAXIAUTO, GO, ATXI, CTOPP, LAND, APT, FOLLOW — all found 2026-09-08). Declare the fired side per verb on `CommandDefinition` like `QueuedDimension`, keep only the instance-sensitive arms in code, and let the registry-wide sweep guard both columns
- [ ] Ground-aircraft queue growth (csharp review 2026-09-08): surface verbs no longer take the clear-everything fast path, so applied ground blocks accumulate ahead of `CurrentBlockIndex` for the life of a taxi and every one is serialized into every snapshot — trim the applied-and-complete prefix when the cursor moves past it; and the exit verbs (`EL`/`ER`/`ET`) now fire `Ground` but are not `IsGroundCommand`, so the dry-run ground guard does not cover them — an exit instruction to a phase-less aircraft clears queued surface blocks and then fails (was: wiped everything)
- [ ] Dry-run fidelity leftovers (csharp review 2026-09-08, after `Dispatch` became a wrapper over `DispatchCompound`): (a) `SplitBlockNonConflicting` has no `SourceCommandText` re-parse fallback like `PatternCommandHandler.BlockParsedCommands`, so a restored block with no `ParsedCommands` that the real path would split-and-keep is dropped whole on the clone; (b) `FinalApproachPhase.DistanceToThresholdNm` is not in its snapshot DTO, so the clone's phase reads `double.MaxValue` — harmless today because `CanAcceptCommand` runs on the real aircraft in `DispatchWithPhase`, but a tower handler consulting it on the clone would wrongly accept a speed inside 5 nm
- [ ] **A deferred or preset `SCAN`/`HSC` mints an unbaked strip id on every run kind** (finding 2026-09-07, C): `SimulationEngine.TickStripDispatches` (the engine's own step since 2026-09-07) fires strip dispatches re-derived from `AircraftState.PendingStripDispatches` rather than replayed from a command, so nothing bakes the Guid it mints — live and a reconstruction hold the same strip under different ids (the same class `StripIdBakingTests` fixed for the typed verbs). Bake it onto whatever records the deferred dispatch, or route the dispatch through the router so the strip arm records it
- [ ] **A same-runway re-taxi silently drops a stored takeoff clearance** (csharp review 2026-09-08, ground-ops): `TryTaxi`/`TryAirTaxi` install a fresh `PhaseList`, so `TAXI … 28R` re-issued to an aircraft holding a mid-taxi `CTO`/`LUAW` for 28R loses it with no warning — the runway-change warning fires only for a *different* runway — and a `TAXI` also drops `LandingClearance`/`ClearedRunwayId` unmentioned; warn (or carry the clearance) on both
- [ ] **`ATXI` can overfly runway pavement en route** (aviation review 2026-09-08, ground-ops item D): `AirTaxiPhase` flies direct from the ramp to the target and nothing coordinates a runway crossing on the way — an air taxi is a ground movement that needs an explicit clearance for every runway it crosses (AIM 4-3-18.a.5 via 4-3-17.b.3; 7110.65 §3-7-2.a.3), carried by §3-11-1.c's `VIA (route)` clause; both KOAK cases happen to stay clear of 28L (`IsOverOrOnPavement` is asserted per tick in `AirTaxiRunwayTerminusTests`), which is not a guarantee elsewhere — warn, or route the air taxi around active pavement
- [ ] `ATXI $7` cannot force the spot reading when a gate shares the name (implementer observation 2026-09-08): `CommandParser.NormalizeAirTaxiDestination` strips the `$`/`@` sigil and `ResolveAirTaxiSpot` tries gate before spot, so at KOAK the names 1, 2, 3, 4, 5, 7, 9, C, E, W always resolve to the gate — carry the sigil through to the resolver as `TAXI` does; `HelicopterAtxiE2ETests`' `[InlineData("ATXI 7A", "taxiway spot, no prefix")]` row is mislabelled for the same reason (7A is a KOAK parking node)
- [ ] `CROSS` at a bar with no cached `Ground.Layout` satisfies the hold and leaves the aircraft phase-less (implementer observation 2026-09-08): `TryPrepareCompletedRouteCrossing` returns `Ok("")` via `IsRunwayHoldShort` when the layout is null and the caller then satisfies the hold-short with no `CrossingRunwayPhase` planned; unreachable from a normal session (every spawned aircraft caches the layout) but silent — and since `WillProduceRunwayCrossing` now shares `HasArrivedAtHoldShort` (route null counts as arrived), the same path attaches an `AfterRunwayCrossing` trigger to a crossing that never happens; resolve the layout from the dispatch context at both sites and fail closed with a refusal
- [ ] Category-aware fillet floor (`NoseWheelTurnRadiusFt`: 25 ft jet / 18 turboprop / 15 piston) — reroutes ~9 % of jet fillets at OAK/SFO, needs the PathfinderGrid/Nightly sweeps and the SFO J133 (#165) case re-judged; also from that review: the intra-fillet no-surge rule in `ArcProfileLimitKts`, `SpeedProfile` sample count vs missed curvature minima, the <45° straight-cut corner speed gap (`CornerSpeedForAngle` 26 kt at 45° vs ~10 kt over the arc)
- [ ] Design the ASDE-X identity-override rule (override identity, never the track key) — yaat-server `live-traffic-swim/08-remaining-work.md` §5
- [ ] Live-traffic follow-ups from the 2026-08-31 aviation review: (a) gate `RunwaySafetyAdvisor.WarnIfTrafficOnFinal` / `WarnIfLiveTrafficOnRunway` on a coasting shadow (7110.65 §5-13-7); (b) validate the three behaviours the receipt-recency fix un-deadened (shadow-vs-simulated conflict alerts, `GroundAcceleration`, assume coast note); (c) surface per-track observation age to the instructor
- [ ] Review the docs structure — user-facing vs internal dev docs (steer 2026-09-02): the root carries USER_GUIDE / COMMANDS / SOLO_TRAINING / GETTING_STARTED / INSTALL beside `docs/`; decide the boundary and where each audience starts
- [ ] Regenerate `docs/scenario-validation-known-failures.md` (last full run 2026-03-12) with yaat-server's `python tools/validate-all-scenarios.py`
- [ ] Hub tests for the non-mentor invite gate (RPO limited access shipped 2026-09-05, plan deleted): an uninvited non-mentor `JoinRoom` is rejected, and a `PullRpo` invite → auto-join round-trips; `TrainingHubAccessHandlerTests` and the kick/invite store tests already cover the rest
- [ ] `StripMutations.RequestDepartureStripForAircraft` / `BuildDepartureStripFields` still carry optional `displayDestinationAirportIds` parameters (pre-existing; the no-optional-parameters rule), and `PrintDepartureStripForAircraft` / `RequestArrivalStripForAircraft` take six positional parameters (the ≤5 limit; the arrival one crossed it when the session clock was threaded through, 2026-09-07) — one `StripPrintTarget` record for the print family fixes both
- [ ] Bounded HOLDP (EFC model) — 7110.65 §4-6-1.c; `HoldingPatternPhase.MaxCircuits` already self-completes, only the HOLDP argument + release path are missing
- [ ] Big-file hotspots to keep in mind when planning parallel work: `PatternCommandHandler.cs`, `MainViewModel.cs`, `CommandParser.cs`, `CommandDispatcher.cs`, `MainWindow.axaml.cs`, `GroundCommandHandler.cs` (3,000–4,000 lines each)

Subplans without a schedule:

- [ ] Standalone airport GeoJSON editor — [airport-editor.md](./airport-editor.md) (51 open; not started)
- [ ] Phraseology coverage — [phraseology-coverage-backlog.md](./phraseology-coverage-backlog.md) + [phraseology-implementation.md](./phraseology-implementation.md) (the handoff doc for the rule backlog)
- [ ] "Show nav route" overlay limitations — [nav-route-overlay-followups.md](./nav-route-overlay-followups.md)
- [ ] BEHIND grammar extensions — [behind-grammar-extensions.md](./behind-grammar-extensions.md) (18 open; deferred by decision)

Closed 2026-09-07 by decision (no scheduled work outside a specific bug report or feature request; promoted to `docs/` as reference): the CRC hub-protocol status table → [`docs/crc-protocol-support.md`](../crc-protocol-support.md), the vTDLS emulation design record → [`docs/vtdls/emulation-design.md`](../vtdls/emulation-design.md), the test-suite speed baselines and TUnit verdict → [`docs/test-suite-speed.md`](../test-suite-speed.md).

## Blockers

None open. (#150's FAA ADX / LADD access was granted and is deployed to YAAT1 — the feed waits only on internal testing.)

## Folder map

| Path | What it holds |
|---|---|
| [tick-path/](./tick-path/README.md) | The tick-path unification programme — one file per step, predicted-vs-got per sub-commit |
| [controller-ai/](./controller-ai/README.md) | Controller AI + soak harness — subdesigns 01–12, the milestone table, the open follow-ups |
| [pilot-ai-self-training/](./pilot-ai-self-training/README.md) | Pilot AI for solo training — the milestone table plus the M11–M12 stubs (shipped M10.x subplans are deleted) |
| [open-issues/](./open-issues/) | Plans for open GitHub issues (#150) |
| the loose `*.md` files | Feature subplans and backlogs that are still open — every one is linked above |
