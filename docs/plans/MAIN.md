# YAAT plans — index
<!-- plan-doc-hygiene: 2026-09-17 0cf9b9ac -->

Entry point for `docs/plans/`. One line per item; the detail lives in the linked subplan. Finished items and finished plans are deleted, never left ticked (steer 2026-09-14) — git history is the record (a plan whose rationale or status table still has reference value is promoted into `docs/` instead). Issue-specific plans live in [`open-issues/`](./open-issues/) and are deleted once implemented.

**Order of work (steer 2026-09-07):** bug reports and feature requests — usually open GitHub issues — come first and ship in hotfix releases. The programmes below them run in the background and release with a non-hotfix. A fresh agent starts at **Bug reports and feature requests**, then the **Current programme**; **Backlog** is unscheduled findings with no report behind them. Folding the open tracker into this file is the `triage-open-issues` skill.

## Bug reports and feature requests

- [ ] **#440 N346G not drawn in the tower cab, visible on SAID** (same bundle): nothing in yaat-server's Tower Cab feed (`CrcVisibilityTracker.GetVisibleTowerCabAirports`, `DtoConverter.ToTowerCab`) or CRC's tower cab gates on transponder Off / no flight plan, and four other stationary t=0 spawns were visible; waiting on the reporter (when it was missing, icon vs tag — Shift+click hides a tower-cab tag). Findings are on the issue. A live repro needs the CRC half of the MCP driver item below
- [ ] **A YAAT-client driving MCP server, committed and standardized in this repo** (steer 2026-09-14): an MCP server under `tools/` that lets an agent drive the desktop client through screenshots, clicks and typing — for reproducing UI-path bugs like #437 end to end instead of only at the store level. The #437 session proved the approach with a PowerShell UI Automation driver, committed as the seed at `tools/drive-client-uia.ps1` (Windows UIA finds Avalonia windows, menu items and buttons by name; ExpandCollapse is unsupported on Avalonia menus so menus need real clicks; the native file dialog is driven with SendKeys; a `YAAT_APPDATA_DIR` scratch dir keeps the developer's preferences untouched). Scope: launch/attach against a scratch appdata dir, window + control enumeration by name/automation id, click, type, screenshot of a window's bounds, log tail; register it in `.mcp.json` and document it in `docs/`. **Also drive CRC** (steer 2026-09-17, from #440): the same server should attach to a running CRC (a WinForms/WPF app, so UIA plus window screenshots should reach it) so one agent can open a CRC display (tower cab, SAID, ASDE-X, STARS) beside the YAAT client and compare what each shows for the same aircraft — the first use is reproducing #440 (N346G visible on SAID, missing from the tower cab)
- [ ] Live-session assume UX (ZOA Discord 2026-08-31): bulk-assume modes, snapshot-then-assume-all, snapshot-as-scenario authoring — yaat-server [live-traffic-swim/09-live-sessions.md](../../../yaat-server/docs/plans/live-traffic-swim/09-live-sessions.md) §3. Auto-assume on first command + `UNASSUME` shipped 2026-09-08 (user pick); the right-click menus still offer only Assume control for a shadow and nothing for `UNASSUME` (the client cannot tell an assumed aircraft from a scenario one — the marker is not on the wire), — noted in §3 (the rollback of an auto-assume the seeded state refuses shipped 2026-09-09)

## Current programme

- [ ] **Tick-path unification** — [tick-path/README.md](./tick-path/README.md): one tick spine, one action router, one body per behaviour across live, replay and reconstruction. Steps 1–3 and 3d shipped (ADRs 0001–0007); step 4 (relocation) is underway — attendance as a recorded input and the track-automation bodies shipped 2026-09-06, the session clock and the strips + TDLS snapshot shipped 2026-09-07, and **shrinking `IActionHost` is underway ([tick-path/04-relocation.md](./tick-path/04-relocation.md), the `IActionHost` item): the TDLS (A), strip (B), coordination (C) and tower-list (D) bodies crossed 2026-09-07 — the coordination verbs, the release-expiry timers, the removal on `TRACK` and the P-list step are engine bodies, a coordination item's id is deterministic, the tower-list dwell entries are snapshotted (schema 23), `IHostSteps`'s step-4 debt is `LiveTrafficSync` alone, and all twelve oracle baselines are empty with the `actions` fixture now scripting coordination; the bookmark, session-clock, CRR-group and ASDE-X/SAID-mutation slots crossed 2026-09-08 (`IActionHost` has one slot left); next is the safety-logic configuration (a design call for the user: opaque `ConfigJson` on the engine vs a Sim-native `AsdexSafetyLogicConfig` projected by `DtoConverter`, sequenced with the `ProcessAsdexAlerts` host step it feeds — the alert set and `LiveTrafficSync` are the two `IHostSteps` bodies left, and `LiveTrafficSync` needs its own subplan)**. Controller AI waits on this (steer 2026-09-02).

## Programmes next up (background; release with a non-hotfix)

- [ ] **Controller AI v1** — on hold behind the tick work; [controller-ai/README.md](./controller-ai/README.md) (H0 / CA0 / CA1 / K1-lite shipped; the open follow-ups are listed there) and the v1 slice [controller-ai/12-milestone-v1-scope.md](./controller-ai/12-milestone-v1-scope.md). The per-frequency radio model ([controller-ai/11-radio-model.md](./controller-ai/11-radio-model.md)) ships first.
- [ ] Pilot AI for solo training — [pilot-ai-self-training/README.md](./pilot-ai-self-training/README.md) (M10.x and Wave 1 shipped; next is Wave 2, M11.2 pilot-initiated requests)
- [ ] Live traffic via SWIM (#150) — [open-issues/150-live-traffic-swim.md](./open-issues/150-live-traffic-swim.md) → yaat-server `docs/plans/live-traffic-swim/08-remaining-work.md`. **In internal testing** (2026-09-07): the FAA ADX / LADD access is granted and the feed is deployed to YAAT1 with the feature flag off; the flag flips when internal testing finishes
- [ ] **Typed command arguments** (steer 2026-09-07) — [typed-command-arguments.md](./typed-command-arguments.md): the registry names each argument's type instead of a prose hint, one small validator per type, arguments bind to record fields by type, and a slot that takes a runway or an altitude is two overloads resolved like a compiler resolves them — the token is tried against each viable overload's declared type (`RunwayArgument`: 1–2 digits + optional L/C/R; `AltitudeArgument`: 3+ digits) — with no combined type; a runway the airport lacks is rejected at dispatch. Step 1 (the pattern modifiers) shipped 2026-09-07; the audit of the remaining slots (step 2) is next

## Backlog — waves

Findings and small items with no subplan and no report behind them, grouped (plan hygiene 2026-09-17) into waves a release can be built around: a wave's items share files and one review gate, so one implementer reads those files once. Take a wave top to bottom, or a whole wave as one release; the Singles share nothing and go one at a time.

### Wave 1 — Same-runway arrival protection follow-ups

Shared files: `SimulationEngine.Generators.cs`, `SameRunwayArrivalProtection.cs`. Gate: `aviation-sim-expert`.

- [ ] Same-runway arrival protection leftovers (aviation review 2026-09-17 of the authority-bound change; both pre-existing): (a) `ReleaseSameRunwayProtection` restores the displaced ceiling silently, with no §5-7-4 advisory line — for a STAR arrival the phrase is "comply with speed restrictions" / "resume published speed" (§5-7-4.b/.c), "resume normal speed" (§5-7-4.a) only where nothing is published; (b) a reduction stamped pre-clearance survives the `CAPP` silently, where §5-7-1.c / AIM 4-4-12.a.7 need it restated to stay in force past the approach clearance — one terminal line at clearance time closes it. Also a citation nit: `SameRunwayArrivalProtection.cs:57-62` cites §5-7-3.f for the wording "reduce to final approach speed"; the paragraph authorises the lower speed, not the phrase.
- [ ] `RNS` to an aircraft slowed on a long final never re-accelerates it (aviation review 2026-09-17, S2-OAK-P arrival-gap hotfix): `FlightCommandHandler.ApplyResumeNormalSpeed` nulls `TargetSpeed` and the ceiling, but `FinalApproachPhase.ManagesSpeed` suppresses the physics auto-schedule and the phase writes no target before its flap gate, so the aircraft holds its reduced speed — AIM 4-4-12.f.1 says "resume normal speed" means return to the normal profile
- [ ] The generator corridor (`SimulationEngine.CorridorAircraft`) measures along-final distance from the pavement threshold, where §5-5-4.h's minima are "over the landing threshold" — on a displaced threshold the in-trail allowance is short by up to ~1.5× the displacement; resolve the leader's distance with `LandingThreshold.Resolve` (aviation review 2026-09-17, minor)

### Wave 2 — Ground realism and braking

Shared files: `AircraftCategory.cs`, `LandingPhase.cs`, `FollowingPhase.cs`, `TugMovePlanner.cs`, `TugKinematics.cs`, `GroundConflictDetector.cs`. Gate: `aviation-sim-expert` (one batch; the retunes depend on each other and desync recordings together).

- [ ] **Rollout braking retune: `RolloutDecelRate` jet 2.5 → ~3.8 kt/s (≈0.20 g, Boeing autobrake 2–3), turboprop 2.0 → ~3.0, piston stays 2.5; replace `LandingPhase.ComfortableBrakingMultiplier` 1.5× with a per-category `CategoryPerformance.ComfortableExitDecelRate` (jet 4.5 / TP 3.75 / piston 3.75) so comfort < firm 5.0 < expedite < RTO stays ordered; decouple `TouchAndGoPhase`'s rollout term with its own constant.** Re-diagnosed 2026-09-17 (design pass over the S1-SFO-2 bundle): **this does not move SFO** — vNAS facility data marks L and P `noTurnoff` on both 28s (user: correct per SOP), so 28 landers take E/T at ~6,200 ft at any rate, and `LandingPhase.TickRollout`'s gentle-decel branch lowers the rate to whatever reaches the far branch point (~2.2–2.5 kt/s measured, whatever the constant says). SFO's occupancy cost was the *vacate* leg (fixed 2026-09-17: the navigator's one-tick overshoot backstop slashed speed at every pass-through node of T's 16-chord polyline). The retune still matters where mid-field exits exist (OAK, MIA, FLL: jets can then take an exit needing 3.75–4.5 kt/s). Owed its own aviation review (per-category default autobrake assumption; one dry default, wet out of scope; the 4.5 comfort figure), moves every landing in every recording (expect replay desyncs in `OakTaxiJcSpinTests`, `ExpediteRunwayExitTests`, `Issue172Jbu577*`, `Issue207LandingRunwayCrossTests`, `ParallelRunwayCrossingAfterExitTests`, `OakFullLifecycleTests` — triage per ADR 0004, never re-baseline), and after it lands re-measure the 14 SFO landings and retune `SameRunwayArrivalProtection`'s `JetIntervalSeconds`/`Turboprop`/`Piston` (70/60/50) from the new mean + 1σ. The §5-5-4.j 2.5 NM relief needs a documented ≤ 50 s average *and* CTRDs/tower-visible turnoffs — keep `TerminalRadarFloorNm` 3.0. Reference: SFO exits from the landing threshold — 28R: N 2,301 · P 3,103 · L 4,287 · E 6,173 · T 6,317 (33° high-speed; its 28R hold bar is 279 ft off the centerline but ~1,010 ft along T) · D 7,968; 28L: N 2,302 · P 3,103 · L 4,287 · E 6,453 · T 7,615 · D 7,960. Two follow-ups seen on the way: `RunwayExitPhase` completes only when the aircraft has *stopped* half a fuselage past the hold bar, ~3 s after the tail is actually clear, even when `AutoPullUpToParallel` then rolls it on to the parallel's bar; and `LandingPhase` delivers UAL2627 to E off 28L at 15 kt (standard-exit speed) while the exit route ceiling is 30 kt
- [ ] `CornerSpeedForAngle` is flat for every piston turn from 90° to 180° (aviation review 2026-09-15, during the CTO-to-roll work): `TaxiTightCornerSpeed(Piston)` = 10 = `TaxiCornerSpeed(Piston)` (`AircraftCategory.cs:1016`, `:1029`), so a light single takes a 90° turnoff at the same speed as a 180° reversal, which no light single does. The helicopter row is 5 vs 10 and does taper. Needs its own regression sweep — every piston ground turn moves
- [ ] `CategoryPerformance.NoseWheelTurnRadiusFt` (25 ft jet) is a turn-centre radius (`wheelbase / tan δ`), not a nose-wheel path radius (`wheelbase / sin δ` ≈ 60 ft for a 737) — rename to say which datum it is, and scale it by weight class: an A388 (wheelbase ~100 ft) cannot do 25 ft (aviation review 2026-09-10)
- [ ] **Ground-speed realism follow-ups** (standstill-acceleration work, 2026-09-09; design in `docs/flight-physics.md` / `docs/ground/navigator.md`; the pushback item (a) shipped 2026-09-17 as the towbar rates): (b) `FollowingPhase.StopDistanceNm` (~90 ft) is closer than a crew sits behind a stopped widebody (AIM 4-3-18.e jet blast) — scale by the lead's weight class; (c) a brakes-held static takeoff (thrust set before brake release; fighters) is not expressible — `GroundRollProfile.For` takes the idle rate and spool from the category, so F15/F16/F18/F22 ramp 1.0 → 10 kt/s like a rolling takeoff; (d) a touch-and-go leader's projected ground run (`PrecedingDepartureBlock`, `RejectedTakeoff.CanOverfly`) integrates the ramp from a standing start and understates the run by base speed × t — conservative for a leader, so left; (e) the rolling takeoff seeds its spool clock from the speed it arrives with; seeding from the line-up's rollout elapsed time would be more faithful
- [ ] **Tug-move follow-ups from the 2026-09-16 aviation re-review** (none blocking; `docs/ground/pushback.md` is the reference): (a) the planner sees no parked aircraft beyond the start-pose overlap refusal, so a `PUSH FACE` or spot move whose swing crosses a neighbour is accepted and then dead-stopped mid-manoeuvre by the outline sweep — feed the parked set into `TugPlanBuilder.Judge` so the candidate ranking picks a template that clears, or refuses; (b) the tug-vs-parked floor shares `WingtipBufferFt` (25 ft) with the mover-vs-mover lateral test, where it sits on top of two half-spans — give the tug rule its own span-scaled constant, `0.05 × span + 10 ft` floored near 10 ft (AC 150/5300-13B taxilane OFA: ~16 ft B738, ~21 ft B77W, ~12 ft C172); (c) `PushbackSpeed`/`PushbackAlignSpeed` are 5/3 kt for every category — a light single or a helicopter on ground-handling wheels tows at ~2 kt; (e) `TightSteeringAngleDeg` 67.5° is type-blind and at/past a B777's connected-towbar limit — scale by type if it ever matters beyond an explicit `PUSH FACE` over 135°; (f) `TugRun.MaxWanderDeg` (120°) sits exactly on a legitimate capture — `D15ToSixAThenD16_…` move 5 measures 120.0° and survives only because the rule is strict — a `ViaLine` commands at most 90° off its line, so the bound should be 90° + the start offset + a margin, derived per run rather than flat; changing it moves candidate ranking, so it needs its own re-judge of the planner suites; (g) the move-boundary half of the ease-off (the within-move half shipped 2026-09-17): `PushbackPhase.HandOver` publishes the ending move's speed and the turn look-ahead sees this move only, so a turn that *opens* the next move is still entered at the boundary speed and braked to the 2.3 kt wingtip cap inside it — ~2.5 s / ~16 ft at the 1 kt/s towbar rate (E6 `PUSH $6B` measured: 4.8 kt at the boundary, 4.1 kt as the nose first moves). Fix: `InstallTugMove` computes each move's opening turning cap from the plan's traces and `HandOver` publishes `min(this move's speed, next opening cap)` (one more `PushbackPhaseDto` field), or the look-ahead takes the run continuation the detector already builds; (h) a cleared tow (`TAXI`/`PUSHM`/`DEL` mid-move) still stops dead in `PushbackPhase.OnEnd` — a braked stop would need a tug-release phase since the next phase drives the other way; `PushbackSpeed` 5 kt exceeds AC 00-65A §11.14's walking pace (~2.6–3.5 kt) and the carry raises the share of a plan spent at it — recorded, not reopened
- [ ] Category-aware fillet floor (`NoseWheelTurnRadiusFt`: 25 ft jet / 18 turboprop / 15 piston / 10 helicopter — shipped in f1cdfedf, 2026-04-23, not by the 2026-09-15 review; #165's SFO J133 case was fixed by the single-pathfinder work) — the open half is re-validating the reroute rate with the PathfinderGrid/Nightly sweeps; also from that review: the intra-fillet no-surge rule in `ArcProfileLimitKts`, `SpeedProfile` sample count vs missed curvature minima, the <45° straight-cut corner speed gap (`CornerSpeedForAngle` 26 kt at 45° vs ~10 kt over the arc)

### Wave 3 — Ground command grammar and dispatch

Shared files: `CommandRegistry.cs`, `GroundCommandParser.cs`, `CommandDispatcher.cs`, `DepartureClearanceHandler.cs`, `GroundCommandHandler.cs`, `HoldingShortPhase.cs`, `docs/command-aliases/atctrainer-commands.json`. Gate: `csharp-reviewer`; `aviation-sim-expert` for the FOLLOWG and cancelled-takeoff rulings.

- [ ] `FOLLOWG` at a runway bar is armed instead of refused (aviation review 2026-09-15, 7110.65 §3-7-2.a.4 "follow (traffic), cross runway …" is issued at the holding position; user 2026-09-15: backlog). Design: `HoldingShortPhase.CanAcceptCommand` returns `Allowed` for `FollowGround` when `ProtectsARunway`; `GroundCommandHandler.TryFollow` on `Allowed` keeps the hold and `Ground.Hold` and replaces the phases after it with the `FollowingPhase` (route stays for the queue/bar logic); the `CROSS` path (`TaxiingPhase.BuildResumePhases` / `BuildPreClearedCrossingPhases`) must keep an armed follow after the crossing instead of rebuilding resume phases; `PhaseList` already persists it. Tests: `PhaseAcceptanceAuditTests` runway-bar theory → `Allowed`; handler test at the 1R/F1 bar (`FOLLOWG` then `CROSS 1R` ends in `FollowingPhase`); a funnel fact with FUN4 `FOLLOWG FUN3; CROSS 1R`. Docs: COMMANDS.md FOLLOWG row, docs/phases.md ground section, CHANGELOG
- [ ] `TAXIAUTO @` / `TAXIALL @` autocomplete offers nothing (csharp review 2026-09-11 of the TAXI/PUSH sigil suggester): both parsers accept `@parking` (`GroundCommandParser` ~458, ~489) but the definitions declare no `CompoundModifiers` (`CommandRegistry` ~896-913), so the metadata-driven sigil path skips them; `TAXIAUTO $` only works because its `TypeHint` contains "spot". Declare the `@`/`$` sigils on both, or give them a stand-typed `TypeHint`
- [ ] Taxiway autocomplete never fires past the first `TAXI` route token (implementer observation 2026-09-11): the route parameter is `R("route", "taxiway names")`, not `Rep(...)`, so `TAXI C D <partial>` offers only the compound-modifier keywords; switching it to repeatable changes the clamp and modifier gating for TAXI, so it needs its own `ArgumentSuggesterTaxiwayTests` pins
- [ ] `PUSH TE @B27` parses `@B27` as a facing taxiway (csharp review 2026-09-11): `GroundCommandParser.ParsePushback` strips the `@`/`$` sigils from the first token only and never rejects one later in the list — refuse a sigil token past the first slot with a message that says where it goes
- [ ] `dmn 50` typed at UAL1486 in the #429 bundle resolved to `CMN 50` (force altitude 5,000) — check the alias/typo-correction table so a `dmn`/`DM` slip cannot become an instructor override
- [ ] `CommandDispatcher` nulls `ControlTargets.TurnRateOverride` in two places (`~:451`, `~:3258`), so a command that fires during a LUAW resumed-mid-turn rollout drops the ground yaw rate `LineUpPhase.ResumeOnAlignedRollout` published and the rest of the swing reverts to the airborne 2.5–3 °/s (2026-09-17, implementer observation; no test covers restore-then-command). Related: `RollingUpgradeMaxRolloutFraction` (0.7 × plan length) now scales with the longer resumed rollout, so a restored aircraft accepts a mid-rollout CTO upgrade over a proportionally longer stretch — also untested.
- [ ] Line-up leftovers from the CTO-to-roll work (2026-09-15): (a) `LineUpPhase.ManeuverSpeedKts` (`?? PathPlan?.ArcSpeedKts`) and `ResumeOnAlignedRollout`'s `TargetSpeed = PathPlan.ArcSpeedKts` still publish the arc-authority speed on segments that are not arcs — neither is on the nose-out path so neither was touched; (b) a rolling line-up whose phase list still holds a pre-satisfied `LinedUpAndWaitingPhase` passes through it for one sub-tick, and `LinedUpAndWaitingPhase.OnStart` publishes `TargetSpeed = 0` on the way past — a ~1.25 kt bite out of the hand-off speed before `TakeoffPhase` seeds its roll clock. Pre-existing, but visible now the hand-off is 10-15 kt rather than 4-7; (c) `DepartureClearanceHandler.CancelTakeoffDuringLineUp`'s other branch (~:1786, an upcoming LUAW phase already exists) sets `HoldPosition` but never `RollingMode = false`, so it never drops the rolling speed floor the way the branch beside it now does
- [ ] A takeoff clearance cancelled inside the stopping distance cannot make the hold-short (aviation review 2026-09-15, flag 2, created by the flow-through change): a cleared aircraft now crosses the bar rolling at up to ~12 kt, and a piston needs ~42 ft to stop from 10 kt at 2 kt/s. `CTOC` at 34 ft stopped with a 0.6 ft margin (`GroundNavigatorRouteEndSpeedTests`); a cancel issued at full flow speed and closer than ~40 ft would not. Decide whether the sim refuses the cancel, warns, or lets the aircraft enter the runway and surfaces it — today it would quietly cross. 7110.65 §3-9-11 (cancellation) and §3-9-4.k (the hold instruction on any amendment to an aircraft holding short) are the governing paragraphs
- [ ] **A same-runway re-taxi silently drops a stored takeoff clearance** (csharp review 2026-09-08, ground-ops): `TryTaxi`/`TryAirTaxi` install a fresh `PhaseList`, so `TAXI … 28R` re-issued to an aircraft holding a mid-taxi `CTO`/`LUAW` for 28R loses it with no warning — the runway-change warning fires only for a *different* runway — and a `TAXI` also drops `LandingClearance`/`ClearedRunwayId` unmentioned; warn (or carry the clearance) on both
- [ ] `CROSS` at a bar with no cached `Ground.Layout` satisfies the hold and leaves the aircraft phase-less (implementer observation 2026-09-08): `TryPrepareCompletedRouteCrossing` returns `Ok("")` via `IsRunwayHoldShort` when the layout is null and the caller then satisfies the hold-short with no `CrossingRunwayPhase` planned; unreachable from a normal session (every spawned aircraft caches the layout) but silent — and since `WillProduceRunwayCrossing` now shares `HasArrivedAtHoldShort` (route null counts as arrived), the same path attaches an `AfterRunwayCrossing` trigger to a crossing that never happens; resolve the layout from the dispatch context at both sites and fail closed with a refusal — half-applied (probe 2026-09-17): `TryPrepareCompletedRouteCrossing` now refuses on a null layout, but `IsRunwayHoldShort` (called first) resolves its own `aircraft.Ground.Layout`, returns false when it is null, and the caller answers `Ok("")` before the refusal is reached, so the refusal is dead code; make `IsRunwayHoldShort` distinguish "no layout" from "not a runway node" or move the null check ahead of it

### Wave 4 — Ground routing, pathfinder and their docs

Shared files: `RouteCostFunction.cs`, `TaxiingPhase.cs`, `GroundNavigator.cs`, `GroundConflictDetector.cs`, `tests/…/TaxiBudgetDeriver.cs`, `docs/ground-layout-generation.md`, `TestData/sfo.geojson`. Gate: `csharp-reviewer`; the `layout-inspect` skill for every repro.

- [ ] **A preserved junction vertex out-scores the fillet arc through the same corner** (UAL58 spot-9 triage, 2026-09-15): at SFO's T9/A junction the graph offers both the raw vertex 33 (plain `T9`/`A` stub edges, kept because T9 runs near-collinear through it) and the `"A - T9"` corner arc `1144 -> 1143`. `RouteCostFunction` charges a `GroundArc`'s own `TurnAngleDeg` sweep unconditionally under `FewestTurns`, so the two-straight walk through the vertex wins (`3.506` vs `4.501`) and the navigator rounds the square pivot itself. Repro: `dotnet run --project tools/Yaat.LayoutInspector -- tests/Yaat.Sim.Tests/TestData/sfo.geojson --pathfinder 1138 T9 A F --pf-dest-rwy 28L --pf-start-heading 117.8 --json`. Related to the "fillets emit parallel-duplicate corner arcs" caveat in `docs/ground/pathfinder.md`
- [ ] SFO taxiway C in the GeoJSON is not straight: ~7° kinks at edge-split nodes with ~38 ft legs make the navigator's turn-rate feasibility cap saw-tooth the speed (12 → 15 → 15.5 → 17.5 → 25 → 20.9 → 30 kt) and brake 2 kt/s on a run with no real corner (`StandstillAccelerationProfileTests` taxi table) — look at the fillet generator / feasibility cap for near-collinear split nodes
- [ ] **`TaxiingPhase.TryStopNoseAtSpot` stops on straight-line distance to the spot node**, not along-route remaining distance (UAL58 spot-9 triage, 2026-09-15), so a route that curves near the spot can complete before the aircraft has driven the remaining pavement. Note the `Segments.Count - 2` arming window is **load-bearing and must stay**: the half-fuselage setback legitimately falls on the penultimate segment for a long aircraft (SFO spot 7A's final leg is 9 ft, so a final-segment-only gate would park a B77W's nose ~112 ft past the spot — issue #234). No repro known; the honest fix is along-route distance, not a segment-index gate
- [ ] `TaxiBudgetDeriver.Derive` plans the route with a hard-coded `AircraftCategory.Jet` regardless of the pair's category (the overheads are per-category since 2026-09-09; the route is not)
- [ ] `NavTickDiag.BrakingLimitKts` / `ArcSpeedLimitKts` are dead stubs (2026-09-17, found while tracing the SFO T-exit vacate): the single `UpdateDiag` call site fills them with `targetSpeed` and `double.MaxValue`, so LayoutInspector `--tick-table`'s cap columns carry no information. Either feed them the real braking/arc caps `GroundNavigator.ComputeTargetSpeed` now hoists into locals (the guarded `[Nav]` log already prints them), or delete the fields and the columns.
- [ ] **Crossing tie-break pins the wrong aircraft when a nose points at traffic its route leads away from** (#224 triage 2026-09-16): `ResolveCrossing` computes closing from `TrueHeading`, so a taxiing aircraft whose route starts past the other's end node (`FindSharedUpcomingNode` finds nothing → `PairKind.Crossing`) but whose nose still points at it is "closing", and `ChooseMutualStopHolder` falls to the callsign ordinal inside `FollowerLeadOffNoseMarginDeg` — the same shape as the original #224. Seen with OAK gate 32 `PUSH TE` ending nose 45° up TE toward SWA1182. Closing should read the route's first segment direction when there is one
- [ ] **A `TAXI` that clears a tow mid-move opens the route with a ~177° entry-align turn that can enter the next fillet ~31 ft off its curve start** (sweep finding 2026-09-17 from the deleted `Issue401ErbTooHighTests` replay): the aircraft is left tail-first, the slow-turn's displacement is still ~32 ft of cross-track when a short (62 ft) first straight "arrives" at its node, and `GroundNavigator.CheckNoTeleport` throws in tests (the app logs and blends). Reachable live: `PUSH`, then `TAXI` before the push finishes. Needs a synthetic repro (OAK 30, `PUSH` then `TAXI via TC U W W1` at ~128 ft into the second leg) and a fix in the route's opening geometry, not in the guard. Related: the #401 replay coverage went with that file — the ERB gate keeps its synthetic tests in `ErbElbNoDistanceTests`, but "an accepted entry is flyable to touchdown" has no synthetic equivalent; a re-record of the S2-OAK session against the current tree restores it (`TestData/issue401-oak-erb-too-high-recording.zip` is kept for that and for `RecordingCorpusRoutingCensusTests`, which still parses it)
- [ ] `docs/ground-layout-generation.md`'s "Fillet Arc Generator" section documents the removed Legacy generator (Phase A–D), not the current `src/Yaat.Sim/Data/Airport/Fillet/` implementation that `docs/ground/fillet-generator.md` covers — rewrite or delete it (found 2026-09-15). Its Phase-D "collinear through-pair forces `preserveNode`" rule still describes real behaviour and needs a home in the current doc

### Wave 5 — Replay, rewind and stuck-taxi repros

Shared files: `RecordingManager.cs`, `TrainingHub.cs` (yaat-server), `LineUpPhase.cs`, `tests/Yaat.Sim.Tests/ModuleInit.cs`, the named recordings. Gate: `csharp-reviewer`; the `bug-bundle` skill for every recording.

- [ ] **Second stuck-exit cause with a layout** (#429 implementer finding, 2026-09-10): replaying `issue429-fat-approaches-recording.zip` **with** the harness `TestData/fat.geojson` still leaves N160TM/N200WM frozen in `RunwayExitPhase` at a standstill for 240 s with no "no exit found … braking to stop" warning — independent of the layout-less fix that shipped; live FAT has no vNAS map today, so users do not hit it. Investigate `TickRolling`/`TryFindExitAhead` before FAT gets a layout
- [ ] In the (now deleted, desynced) `CtoMrdPatternExitTests` recording, after the recorded `CTO MRD` binds to runway 15 N784ME sits in `LineUpPhase` stationary from t=553 to t=1000 without reaching `TakeoffPhase` — a stuck-line-up smell worth its own repro. **Re-check against the 2026-09-15 line-up work before spending time on it**: two things that shipped then produce exactly "stationary in `LineUpPhase` forever" — a `LineUpGraphRoute.TryPlan` refusal at a runway-crossing taxiway that faulted the synthetic plan (`TickFaulted` holds speed 0 indefinitely), and a `HoldPosition` that never cleared. The recording is gone, so this needs a fresh repro either way; N784ME is also the aircraft the hold-gap defect reproduced on at OAK
- [ ] **A hybrid replay restored mid-Bézier trips the no-teleport invariant** (UAL58 spot-9 triage, 2026-09-15): `RestoreFromSnapshot` at t=925 of `ual58-spot9-reversal-recording…zip` throws `[Nav] teleport: UAL2627 moved 4.0 ft in one sub-tick on a Bezier that advanced 1.3 ft` at t=931 (`GroundNavigator.CheckNoTeleport`, `:966`, via `TickBezier`). The navigator's arc-playback state is deliberately not snapshotted (`docs/ground/navigator.md`, "Snapshot round-trip is intentionally lossy"), so re-deriving it mid-arc can write a position discontinuity on the first sub-tick. `ThrowOnTeleport` is set assembly-wide in `tests/Yaat.Sim.Tests/ModuleInit.cs:24` with no sanctioned per-test opt-out, so every hybrid-replay test has to dodge the restore points where some aircraft happens to be mid-fillet (this one moved to t=920). Either blend the first sub-tick after a restore, or re-anchor the arc on the restored pose
- [ ] **A rewind whose nearest snapshot lies after a `DEL` on a shadow still un-suppresses it** (implementer observation 2026-09-08, DEL-shadow fix): `RecordingManager.RewindAsync` restores from `FindNearestSnapshot` and reconstructs only from that second on, so the `RecordedLiveTrafficRemoval` before the snapshot is never replayed and `RoomLiveTrafficState.Suppressed` (room state, not snapshotted) stays empty — needs the suppression set in the room checkpoint/snapshot or a carry-over across the reload, the same shape as the bookmarks carry-over
- [ ] `RecordingManager.RewindAsync` and `RewindFromSnapshotAsync` are ~90 % identical bodies (csharp review 2026-09-09; the resync omission was exactly this duplication): extract `ExecuteRewindAsync(RewindPlan)` with a required-member `RewindPlan` (snapshot, from-second, target, terminal line) and settle deliberately whether `PlaybackEndSeconds` is set before or after the reconstruction (the two differ today; inert until the reconstruction reads it)
- [ ] `LoadRecordingArchiveAsync` / `LoadRecordingAsync` tear down and rebuild the engine under suppression but never run `ResyncEngineStateAsync` (csharp review 2026-09-09): an instructor with printed strips and PDCs who loads a bundle keeps the abandoned run's strips on every client until something ticks; `LoadRecordingAsync` has a comment justifying it, the archive path has none — route both through the resync or document why not
- [ ] **A CRC position change made during playback is applied but not recorded** (fallout of the 2026-09-15 play-button fix): `SyncCrcPositionToRpo` now goes through `RoomEngine.IssueAutomatic` so it cannot truncate the tape, but while the room is in playback the replay run profile swallows the `AS {code}` append — so a later rewind reverts the selection to whatever the tape says. Closing it means re-issuing the room's CRC selections when the room leaves playback, next to the `Engine.SyncAttendance()` that solves the same problem for attendance in `RecordingManager.LeavePlayback` — which needs a hub-side hook, since `RecordingManager` has no CRC client registry (`TrainingHub.SyncAllCrcPositionsInRoom` is the body to reuse; today it runs only on scenario load)
- [ ] The `sfo-gc-arrival-goarounds` recording logs `No dispatcher arm for PushbackCommand (Pushback onto M4) on KLM606` / `(Pushback onto T7B) on SKW5707` → `[Preset] … could not apply`, `No dispatcher arm for TaxiCommand (Taxi via T7B to spot 7B)`, `Runway lookup failed for N346G: runway 'E' not found at SFO`, and `Procedure leg fix 'RW28R' (CF) not found in CIFP terminal waypoints` on every replay (2026-09-17). Decide whether each is a replay-fidelity gap (a recorded action the reconstruction cannot apply) or noise to silence; none is exercised by a test today.

### Wave 6 — Command queue and dispatch architecture

Shared files: `FlightPhysics.cs` (`UpdateCommandQueue`), `CommandDescriber.cs`, `CommandDispatcher.cs`, `DispatchContext.cs`, `SimulationEngine.Commands.cs`. Gate: `csharp-reviewer`; `docs/command-chaining.md` is the contract.

- [ ] **The command queue has one cursor for independent chains** (implementer finding 2026-09-09, takeoff-dims): a block a fresh compound preserves and the compound's own chain share one list, so whichever is first can hold the other — preserved-first (shipped) lets a pre-armed `AT 1000 CM 5000` fire but makes `TAXI D; SQ 1234` wait behind an unmet `AT 5000 CM 7000` until 5,000 ft. Scope "hold behind an unfired predecessor" to blocks of the same dispatch (`CommandBlock.SourceCommandText` is already the per-dispatch key `DiscardChainRemainder` groups by) at the three advancement sites: `ApplyReadyConditionalBlocks` (record the blocker's text and `continue` instead of `break`), `AdvanceQueueWhileIdle`'s `frontierBroken`, and the regime-A cursor in `UpdateCommandQueue` (a per-chain frontier rather than one cursor — the deep one); pin both counter-cases
- [ ] **A `FiredDimension` registry column** (csharp review 2026-09-08): `GetCommandDimension` is a thirteen-arm predicate ladder (cyclomatic ~20 against the ≤8 limit) and every new verb outside a family predicate has fallen to `None` and wiped the queue (RWY, TAXIAUTO, GO, ATXI, CTOPP, LAND, APT, FOLLOW — all found 2026-09-08). Declare the fired side per verb on `CommandDefinition` like `QueuedDimension`, keep only the instance-sensitive arms in code, and let the registry-wide sweep guard both columns
- [ ] Ground-aircraft queue growth (csharp review 2026-09-08): surface verbs no longer take the clear-everything fast path, so applied ground blocks accumulate ahead of `CurrentBlockIndex` for the life of a taxi and every one is serialized into every snapshot — trim the applied-and-complete prefix when the cursor moves past it; and the exit verbs (`EL`/`ER`/`ET`) now fire `Ground` but are not `IsGroundCommand`, so the dry-run ground guard does not cover them — an exit instruction to a phase-less aircraft clears queued surface blocks and then fails (was: wiped everything)
- [ ] Dry-run fidelity leftovers (csharp review 2026-09-08, after `Dispatch` became a wrapper over `DispatchCompound`): (a) `SplitBlockNonConflicting` has no `SourceCommandText` re-parse fallback like `PatternCommandHandler.BlockParsedCommands`, so a restored block with no `ParsedCommands` that the real path would split-and-keep is dropped whole on the clone; (b) `FinalApproachPhase.DistanceToThresholdNm` is not in its snapshot DTO, so the clone's phase reads `double.MaxValue` — harmless today because `CanAcceptCommand` runs on the real aircraft in `DispatchWithPhase`, but a tower handler consulting it on the clone would wrongly accept a speed inside 5 nm
- [ ] `DispatchContext` has 15 positional record parameters (csharp review 2026-09-08; the ≤5 cap): every new field touches six engine construction sites — move to init-only properties with object-initializer construction so the next field is a one-line change
- [ ] Every flight-plan amendment now re-broadcasts the aircraft's vTDLS item (2026-09-08), including the high-frequency `CrcImpliedBareSlew` owner-slew beacon accept and empty amendments (the revision counter bumps on a no-op by design): decide whether the mark should skip amendments that changed no header field, or whether the per-item push is cheap enough to leave

### Wave 7 — Strips, TDLS, air-taxi and hub

Shared files: `SimulationEngine.Strips.cs`, `StripMutations.cs`, `AirTaxiPhase.cs`, `CommandParser.cs`, `CrcClientState.Stars.cs` (yaat-server), `RpoJoinGateTests.cs` (yaat-server). Gate: `csharp-reviewer`; `aviation-sim-expert` for the ATXI runway rule.

- [ ] **A deferred or preset `SCAN`/`HSC` mints an unbaked strip id on every run kind** (finding 2026-09-07, C): `SimulationEngine.TickStripDispatches` (the engine's own step since 2026-09-07) fires strip dispatches re-derived from `AircraftState.PendingStripDispatches` rather than replayed from a command, so nothing bakes the Guid it mints — live and a reconstruction hold the same strip under different ids (the same class `StripIdBakingTests` fixed for the typed verbs). Bake it onto whatever records the deferred dispatch, or route the dispatch through the router so the strip arm records it
- [ ] `StripMutations.RequestDepartureStripForAircraft` / `BuildDepartureStripFields` still carry optional `displayDestinationAirportIds` parameters (pre-existing; the no-optional-parameters rule), and `PrintDepartureStripForAircraft` / `RequestArrivalStripForAircraft` take six positional parameters (the ≤5 limit; the arrival one crossed it when the session clock was threaded through, 2026-09-07) — one `StripPrintTarget` record for the print family fixes both
- [ ] **`ATXI` can overfly runway pavement en route** (aviation review 2026-09-08, ground-ops item D): `AirTaxiPhase` flies direct from the ramp to the target and nothing coordinates a runway crossing on the way — an air taxi is a ground movement that needs an explicit clearance for every runway it crosses (AIM 4-3-18.a.5 via 4-3-17.b.3; 7110.65 §3-7-2.a.3), carried by §3-11-1.c's `VIA (route)` clause; both KOAK cases happen to stay clear of 28L (`IsOverOrOnPavement` is asserted per tick in `AirTaxiRunwayTerminusTests`), which is not a guarantee elsewhere — warn, or route the air taxi around active pavement
- [ ] `ATXI $7` cannot force the spot reading when a gate shares the name (implementer observation 2026-09-08): `CommandParser.NormalizeAirTaxiDestination` strips the `$`/`@` sigil and `ResolveAirTaxiSpot` tries gate before spot, so at KOAK the names 1, 2, 3, 4, 5, 7, 9, C, E, W always resolve to the gate — carry the sigil through to the resolver as `TAXI` does; `HelicopterAtxiE2ETests`' `[InlineData("ATXI 7A", "taxiway spot, no prefix")]` row is mislabelled for the same reason (7A is a KOAK parking node)
- [ ] `CrcClientState.ReadSharedStateWire` sits at cyclomatic ~26 (lifted verbatim 2026-09-09; the ≤8 cap): two static local helpers over `ref MessagePackReader` (`ReadOptionalString`, `ReadEnumOrInt<T>`) collapse keys 0–3, 6 and 9
- [ ] Hub tests for the non-mentor invite gate (RPO limited access shipped 2026-09-05, plan deleted): an uninvited non-mentor `JoinRoom` is rejected, and a `PullRpo` invite → auto-join round-trips; `TrainingHubAccessHandlerTests` and the kick/invite store tests already cover the rest

### Wave 8 — Client UI cleanup

Shared files: `UserPreferences.cs`, `RadarDatablockLayout.cs`, `GroundViewModel.cs`, `RadarViewModel.cs`, `SettingsWindow.axaml.cs`, `NavRouteOverlayProjector.cs`. Gate: `csharp-reviewer`.

- [ ] Client cleanup noticed while shipping #434/#435/#436 (2026-09-14): (a) `UserPreferences.SavedPattern.Verb` is a legacy single-verb field still read by the scheme loader beside `Aliases` — delete it and the fallback; (b) `RadarDatablockLayout.Compute` takes 7 positional parameters (the ≤5 limit) even after `DatablockOverlays` — fold `blockX`/`blockY`/`style`/`showNoLandingClearance` into a layout-request struct; (c) `EuroScopeTagLayout` has no ATPA in-trail line, so the ATPA toggle only affects the STARS-style datablock; (d) `ApplyWindowProfilePartialAsync` (Copy View Settings with pop-outs) flips Terminal/DataGrid/Ground/Radar but not Controllers/Metar — the full apply flips all six; (e) `SettingsWindow.OnImportMacrosClick` swallows `JsonException` with no log or user note, unlike the verbs importer beside it; (f) `GroundViewModel`/`RadarViewModel` constructors still carry optional `onSelectionChanged`/`preferences` parameters (the no-optional-parameters rule) — every production caller passes them
- [ ] Nav-route overlay: distinguish active from latent published restrictions (AIM 5-2-9 / 5-2-9.10 / 5-2-9.11 — needs clearance state the CIFP data lacks) and draw a racetrack for a hold held only as a route fix or by `AirspaceBoundaryHoldPhase` / `VfrHoldPhase`; the shipped behaviour and both gaps are in `docs/radar-rendering.md` § Nav-route overlay wire contract (`NavRouteOverlayProjector.BuildShapes`)

### Wave 9 — Live traffic

Shared files: `RunwaySafetyAdvisor.cs`, yaat-server `LiveTraffic/`, `docs/plans/live-traffic-swim/08-remaining-work.md`. Gate: `aviation-sim-expert`.

- [ ] Live-traffic follow-ups from the 2026-08-31 aviation review: (a) gate `RunwaySafetyAdvisor.WarnIfTrafficOnFinal` / `WarnIfLiveTrafficOnRunway` on a coasting shadow (7110.65 §5-13-7); (b) validate the three behaviours the receipt-recency fix un-deadened (shadow-vs-simulated conflict alerts, `GroundAcceleration`, assume coast note); (c) surface per-track observation age to the instructor
- [ ] Design the ASDE-X identity-override rule (override identity, never the track key) — yaat-server `live-traffic-swim/08-remaining-work.md` §5

### Wave 10 — Docs and repo hygiene

Shared files: root `*.md`, `docs/scenario-validation-known-failures.md`, solution-wide `dotnet format`. Gate: none.

- [ ] Review the docs structure — user-facing vs internal dev docs (steer 2026-09-02): the root carries USER_GUIDE / COMMANDS / SOLO_TRAINING / GETTING_STARTED / INSTALL beside `docs/`; decide the boundary and where each audience starts
- [ ] Regenerate `docs/scenario-validation-known-failures.md` (last full run 2026-03-12) with yaat-server's `python tools/validate-all-scenarios.py`
- [ ] **Strict C# formatting, both repos** (2026-09-18, underway) — [strict-formatting.md](./strict-formatting.md): delve-the-dungeon's style rules as build errors (`EnforceCodeStyleInBuild`, var only when apparent), every existing violation fixed in rule-family commits (IDE0008 alone is ~36k sites), then `dotnet csharpier check` + `dotnet format --verify-no-changes --severity info` in CI and the prek hooks at info severity
- [ ] Adopt `AnalysisLevel=latest-recommended` (deferred from the strict-formatting pass by decision, 2026-09-18): ~12k CA hits measured — CA1707 8,938 (underscores in test names: switch off under `tests/`), CA1848 1,471 + CA1873 805 (logging delegates / guarded logging), CA1305 319 (culture), CA1859 103, CA1310 92, CA1822 76, the rest ~300. Work it rule by rule, each raised to `warning` in the commit that clears it

### Singles

Shared files: no shared files. Gate: per item.

- [ ] **Descent ladder is speed-blind** (aviation review, #429, 2026-09-10): the A319 3,500 fpm below FL100 is ~4.8° at 290 KIAS but 6.5–7.5° at 210–250 KIAS, and the step planner's `standardRate * 2.0` cap reaches ~6,300 fpm — prefer a descent-angle cap in `UpdateDescentPlanning` / `AircraftPerformance.DescentRate`
- [ ] **Unexplained RWY 30 landing stall in a live continuation** (S2-OAK-P arrival-gap hotfix, 2026-09-17, unverified): restoring the bundle's t=2055 snapshot into a live engine with auto-cleared-to-land on and no recorded actions, three arrivals land on OAK 30 and then none do for the remaining ~25 min — identical on the unfixed base, so not from the hotfix. Nothing was on runway 30 at the restore point; suspect a lander stopping on the runway with no taxi instruction plus auto go-around, but no go-around was logged by the harness. Needs a harness that records phases/go-arounds per arrival before any conclusion
- [ ] Bounded HOLDP (EFC model) — 7110.65 §4-6-1.c; `HoldingPatternPhase.MaxCircuits` already self-completes, only the HOLDP argument + release path are missing
- [ ] **Preferred taxi routes should drive `TAXIAUTO`** (#426 airport sidecar ideas; steer 2026-09-09): when a sidecar lists a preferred taxi route for a destination runway (e.g. OAK 28R: `D C B`), an aircraft that can execute the whole route from its present position, or join it part-way (`C B`), should prefer it over the naive layout traversal — with an implicit preamble (ramp / non-movement taxilane) to reach the first listed taxiway. Also in #426: strip push points in preferred routes, gate operator/type/weight and pushback data, hand-drawn curved taxiways replacing generated ones, ramp-vs-ground frequency areas

Subplans without a schedule:

- [ ] Standalone airport GeoJSON editor — [airport-editor.md](./airport-editor.md) (51 open; not started)
- [ ] Phraseology coverage — [phraseology-coverage-backlog.md](./phraseology-coverage-backlog.md) + [phraseology-implementation.md](./phraseology-implementation.md) (the handoff doc for the rule backlog)
- [ ] BEHIND grammar extensions — [behind-grammar-extensions.md](./behind-grammar-extensions.md) (18 open; deferred by decision)
- [ ] SFO ground-technique test coverage — [sfo-ground-technique-tests.md](./sfo-ground-technique-tests.md): the ZOA SFO GND familiarization video (2026-09-15) mapped technique-by-technique onto YAAT commands and SOP v1.11; 13 pathfinder probes (the L/M2 hold has no junction; the two 1L-via-B routes shipped as the connector fold 2026-09-15), a flagship 28/28 departure-funnel test, ramp choreography, per-configuration route pins, three grammar gaps, seven `KSFO.json` facility-rule candidates — top of the ground-technique queue (user 2026-09-15): B4 `RES HS B1` in `Issue394HoldShortSpotTests` and A2 in `SfoYankeePushTests` are its next two tasks

Closed 2026-09-07 by decision (no scheduled work outside a specific bug report or feature request; promoted to `docs/` as reference): the CRC hub-protocol status table → [`docs/crc-protocol-support.md`](../crc-protocol-support.md), the vTDLS emulation design record → [`docs/vtdls/emulation-design.md`](../vtdls/emulation-design.md), the test-suite speed baselines and TUnit verdict → [`docs/test-suite-speed.md`](../test-suite-speed.md).

## Blockers

None open. (#150's FAA ADX / LADD access was granted and is deployed to YAAT1 — the feed waits only on internal testing.)

## Folder map

| Path | What it holds |
|---|---|
| [tick-path/](./tick-path/README.md) | The tick-path unification programme — one file per step, predicted-vs-got per sub-commit |
| [controller-ai/](./controller-ai/README.md) | Controller AI + soak harness — subdesigns 01–12, the milestone table, the open follow-ups |
| [pilot-ai-self-training/](./pilot-ai-self-training/README.md) | Pilot AI for solo training — the milestone table plus the M11–M12 stubs (shipped M10.x subplans are deleted) |
| [open-issues/](./open-issues/) | Plans for open GitHub issues (#150) |
| the loose `*.md` files | Feature subplans and backlogs that are still open — every one is linked above |
