# YAAT plans — index

Entry point for `docs/plans/`. Each row links a subplan; open/done counts are the subplan's own checkboxes at the time of the last edit here.

> **Active milestone: controller AI.** The whole-feature map is
> [`controller-ai/README.md`](./controller-ai/README.md); the ordered slice being built now — one tick
> spine, per-frequency radio, CA2 Tower brain, H1 soak, 7 phases and 29 requirements — is
> [`controller-ai/12-milestone-v1-scope.md`](./controller-ai/12-milestone-v1-scope.md).

A fresh agent should start from **Current focus**, then **Next up**, and treat **Backlog** as unscheduled. Finished plans move to
[`archive/`](./archive/); issue-specific plans live in [`open-issues/`](./open-issues/) and are deleted once implemented.

## Session 2026-09-05 — Discord server-cost tracker

- [x] Ko-fi-fed progress ticker + embed and supporter roles in Discord — shipped and live 2026-09-05; decision record in [archive/discord-support-tracker.md](./archive/discord-support-tracker.md), operating doc `docs/discord-integration.md`

## Session 2026-09-01 — S2-OAK-2 bundle (LUAW after touchdown, taxi corners)

- [x] A. LUAW 3-9-4.c warning ends at touchdown (`IsClearanceStillPendingUse` = airborne); advisories stop repeating the callsign — committed `d27971a9`.
- [x] A′. Follow-up from review: reword the advisor doc comments (3-9-4.a is clearance-agnostic and "contemplates"; 3-9-6.b protects the LUAW aircraft at takeoff-clearance time; drop the 3-9-5 reasoning in code and in the theory test comment); add a 3-9-6.b advisory when a takeoff clearance is issued over a *simulated* landed arrival still on the pavement (`RunwayOccupancy.Classify`, excluding occupants the LUAW/holding branches already count); citation tweaks (§3-10-3.a for stopped traffic, 3-10-5.e only for a holding occupant); MTR handler callsign prefix already stripped in the tree.
- [x] C. Fillet corners — committed `a1b69a2d` ("fix: taxi corners follow the painted fillet"), which is also the commit that added this block, so the box was never ticked: reverse-arc penalty removed; arc sweep in the turn budget; Fastest priced by traversal time + corner dip; local-curvature arc speed profile; A* closed set keyed by arrival taxiway; reach-cost detours priced with the search's own cost; fillets below the 10-ft steerable floor inadmissible; navigator releases the physics heading hold when a straight takes over. Guard `RouteGeometryAsserts` on every coverage pair.
- [ ] C′. Category-aware fillet floor (`NoseWheelTurnRadiusFt`: 25 ft jet / 18 turboprop / 15 piston): reroutes ~9 % of jet fillets at OAK/SFO, needs the PathfinderGrid/Nightly sweeps and the SFO J133 (issue 165) case re-judged. Also from review: intra-fillet no-surge rule in `ArcProfileLimitKts`; `SpeedProfile` sample count vs missed curvature minima; the <45° straight-cut corner speed gap (`CornerSpeedForAngle` 26 kt at 45° vs ~10 kt over the arc).
- [x] D. Post-corner wiggle: `LookAheadFloorFt` (10 ft) → `CategoryPerformance.NoseWheelTurnRadiusFt(category)` in `TickStraight`; test `SmallOffset_ReacquiresWithoutHeadingHunt`; OAK tick-table comparison.
- [ ] Wrap-up: `test-all.ps1 -Full`, `architecture-updater`, memory notes, delete `.tmp` scratch.

## Current focus (session 2026-09-01 — controller AI, milestones H0 → CA0)
- [x] **H0 — headless host + soak-runner skeleton (no AI)** — shipped 2026-09-01; see [controller-ai/README.md](./controller-ai/README.md) milestone table + [07-soak-runner.md](./controller-ai/07-soak-runner.md)
  - [x] yaat: `SimulationEngine.DeleteAircraft` stamps `CompletionReason.Dropped` on DEL (live + replay) + `Yaat.Sim.Soak.CapturingSimLogProvider`
  - [x] yaat-server: seeded scenario-load core (all three RNG streams seeded), `RoomEngine.AdvanceLiveSecond`, `ServerDataBootstrap`, `Simulation/Headless/HeadlessRoom` (+ `CollectingTrainingBroadcast`, null stubs), `Soak/` episode loop + recording sink, `tools/Yaat.SoakRunner run`, determinism tests; `Yaat.RecordingUpgrader` migrated onto the host
  - [x] Measured ~420× realtime (README H0 row)
  - [x] Replays stay deterministic across calendar time (steer 2026-09-01): `SimScenarioState.MagneticModelDateUtc` (snapshotted, in the recording manifest + room checkpoint, restored on load); pre-feature recordings fall back to the manifest's `RecordedAtUtc` day. Residual: FRD resolution inside the command parser still uses the process day (no scenario in scope)
- [x] **CA0a — `DispatchOrigin` + pilot-contact roster** (AI positions are student stand-ins: pilots call whichever answering position is responsible; `HasMadeInitialContact` stays student-scoped, AI contact latched per position id; drain gates key on `PilotContacts.AnyAnswering`) — shipped 2026-09-01 after the aviation review (must-fix: tower-cab AI positions now match the physical airport, not the filed destination; AI candidates obey the SOP transfer rules; ground calls fall back to an AI tower working alone; airborne check-in skips the tower's arrival side; staffing version bumps on any config change)
  - [x] Incidental fix: `TAXIAUTO` now satisfies a pending "ready to taxi" request (solo students using it were nagged every 2 min)
  - Review follow-ups (not blocking CA0b):
    - [ ] Partition `FrequencyState` per answering position (or airport + type) and route `Enqueue`/`ExpectReadback`/`AcknowledgeControllerResponse` through the resolved addressee — today an AI Ground's answer clears the awaiting-response gate held for a pilot waiting on tower (CA2/CA3)
    - [ ] Same-type AI positions at one airport (LC1/LC2, GC1/GC2 split by runway or area) need a runway/area predicate before multi-position staffing; `ResolveFor` takes the first by position id today
    - [x] AI Ground transfers the pilot to tower (`CT`) before the hold-short — 7110.65 §2-1-17.a — `HandToLocalRule` (CA1, 2026-09-01)
    - [ ] `AiPositionResolver` (CA0b): keep `_DEL` out of Ground by default so a taxi request is never addressed to "… Clearance Delivery", and give a `DEP` position its own "… Departure" call name (AIM TBL 4-2-1) instead of the `APP` fold
    - [ ] `HasLeftStudentFrequency` (`ContactCommandHandler.Route`) is one bool for a multi-position world — revisit when the AI issues `CT` (CA2)
- [x] **CA0b — controller-AI core + observer brains** — shipped 2026-09-01 (`AiPositionResolver`, `ControllerAiConfig`, staffing, `PositionJurisdiction`, `AiAnomalyLog`, `AiControllerService`, sinks, the four watchdog rules + `ObserverBrain`, `TickControllerAi` from `AdvanceLiveSecond`, SoakRunner `--positions`, routing-parity spike, determinism tests). Aviation-reviewed: ordered stops and departure-queue yields no longer read as stuck; a request is overdue only once the pilot has re-asked; held-for-release takeoffs exempt; partial staffing falls through to the radar owner; taxiing along a runway is Local's
  - Review follow-ups (not blocking CA1):
    - [ ] When CA1 skips `ProcessAutoAccept` for AI radar positions, land the AI `ACCEPT` rule in the same change — otherwise every handoff to an AI radar position trips `HandoffUnaccepted` at `AutoAcceptDelay` + 60 s
    - [ ] `ConflictAlertInAiJurisdictionRule` inherits the detector's VFR-to-VFR pattern-entry pairs (converging toward a 45° downwind entry 3+ nm out, where see-and-avoid / tower visual separation §7-2-1 apply) — needs a detector-level VFR pattern exemption before the safety tier is trusted at a VFR field
    - [ ] Multi-position CA: a pair split across two AI positions opens one episode per position by design; the findings aggregator (H1) should fold them by conflict id
    - [ ] `AiPositionResolver.FindCabFacility` falls back to a callsign-prefix match for combined ATCT/TRACON facilities whose id is not the airport's — verify against a real one (FAT, MC1) when those scenarios are soaked
- [x] **CA1 — Ground brain v1** — shipped 2026-09-01; see the [controller-ai README](./controller-ai/README.md) CA1 row + [04-ground-brain.md](./controller-ai/04-ground-brain.md) implementation notes. Also landed: the arrival's taxi-in call (`TaxiInRequest`, pilot-picked parking), origin-neutral `CT`, `RunwayInUse` knob + SoakRunner `--runway`, `CoordinationTimeout`
  - [x] Steer 2026-09-01: arrivals leave at their parking spot when the scenario keeps spawning traffic — `SimScenarioState.EffectiveAutoDeleteMode` derives `Parked` from `HasOngoingTrafficSource` (timed spawns or generators) when the author left `autoDeleteMode` unset; static scenarios keep parked arrivals for turnaround; Settings still overrides; the pure-engine test host now runs `TickAutoDelete` like the server
  - Soak findings (H1 detector work, not CA1 bugs):
    - [ ] OAK give-way gridlock: with S1-OAK-2/3/4's arrival generators on, arrivals exiting 30 taxi in along W against departures taxiing out on W; the reactive give-way physics locks the opposite flows head-on within ~20 min. Ground v1.1 flow sequencing (route arrivals/departures on different taxiways, or hold one flow) — plan 04 rule 3
    - [ ] `VirtualNode` ids come from a process-wide counter, so two runs in one process (or two processes) label hold-short setback / ramp-lane nodes differently in snapshots; the determinism tests mask negative node ids. A per-engine counter would make same-seed snapshots byte-identical across runs (H5 `verify`)
    - [ ] A parked *arrival* that is not deleted re-announces ready-to-taxi as a turnaround (`AtParkingPhase` does not distinguish arrivals); fine for static scenarios, worth a `CompletionReason` reset when it happens
  - Review follow-ups (not blocking K1-lite):
    - [ ] Generic calm-wind fallback is by designator (OAK ⇒ `12`); a scenario-observed configuration could replace it at knowledge-less airports
    - [ ] AI `CT` readback lands on the RPO lane in instructor rooms — folded into the per-frequency milestone
    - [ ] `AutoPullUpToParallel` stays false headless (H0 default); flip when the soak should exercise the §3-10-9.b.2 path
    - [ ] The rule-2 request line with an observer Local is by design until CA2's coordination bus (`--positions GC,LC` collects `CoordinationTimeout`s and its arrivals wait for Local's `CT` to ground; the CA1 soak runs `--positions GC`)
- [x] **K1-lite — facility SOP knowledge, runway-selection subset** — shipped 2026-09-01; `FacilityOps` schema + `FacilityOpsDatabase` + `KOAK.json` (OAK ATCT SOP 1-6 / 4-2 / 3-4) + `SopAircraftClassifier` + `FacilityRunwaySelector`/`Assigner` + `RunwayUsabilityGate` + `ControllerAiConfig.RunwayConfigurations` (SoakRunner `--runway-config`); see [`docs/facility-ops-knowledge.md`](../facility-ops-knowledge.md) and the [10-facility-knowledge.md](./controller-ai/10-facility-knowledge.md) K1 row
  - [ ] K1b with CA2: the Tower-brain sections of `KOAK.json` (departure tables, release rules, missed-approach tables, crossing approvals, pattern preferences)
  - [ ] Generic runway usability by pavement length vs aircraft category belongs in `RunwayUsabilityGate` too (aviation review; today the gate is tailwind-only)
  - [ ] Crosswind gate (§3-5-1.c pairs tailwind *and* crosswind; SFO 1-5's waiver is itself a crosswind number) and gating of a configuration's *arrival* runways — latent at OAK, where every configuration's arrival set equals its departure set
  - [ ] A session `--runway-config` that fails the gate is kept as set with an informational `KnowledgeConflict`; decide whether the runner should refuse it outright
  - Aviation-review follow-ups (2026-09-01; the must-fixes — go-around over the runway closes the gate, taxi-in waits for the tower's release — are in):
    - [ ] Re-check the crossing gate every tick between the clearance and the wheels crossing the markings and convert to a hold when the picture changes (§3-7-2.a.7.2: the conditions apply at crossing time, not clearance time)
    - [ ] "Passed the crossing point" relief: a departure or arrival past the crosser's intersection no longer blocks it (§3-7-2.a.7.1, .a.7.2.2.4) — today any Departing/OnSurface occupant anywhere on the pavement blocks, a likely contributor to the W gridlock
    - [ ] `CROSS <rwy> AT <twy>`: the canonical carries no crossing point, so the readback lacks "at Bravo" (§3-7-2.a.1.1, §3-1-3.b); pre-existing grammar, now with the AI as issuer
    - [ ] Generic runway usability: filter runway ends by pavement length against the aircraft category before ranking by wind (§3-5-1.b.1) — fold into K1-lite's `RunwayUsabilityGate`
    - [ ] A crosser under `Ground.Hold` is refused by the gate forever and the stuck watchdog ignores ordered stops — a held aircraft stalls silently; the brain never issues `RES`
    - [ ] §3-1-3.c completion report when a human Local clears the crossing (CA2's bus)
    - [ ] Prefer the runway-in-use end's name in `CROSS`/hold-short reports on an active runway instead of the nearest threshold (cosmetic; pilot and controller already agree)
- [ ] Captured steer (CA3/H4): when watching the AI live with TTS on, each AI position speaks its own instructions with a randomly assigned unique TTS voice (`BroadcastPilotTransmission` speaker id + a controller-phraseology verbalizer)
- [ ] **Captured steer (2026-09-01) — per-frequency radio model, a prerequisite for two AI positions talking to pilots at once (CA2: AI Ground + AI Local together, coordinating off-frequency).** Today one shared `SimulationWorld.ActiveFrequency` serializes every pilot and controller transmission. Sketch: (1) `AircraftState.TunedPosition` (position key; set at spawn by the responsible position, by `CT`, by the AIM 4-3-14.a self-switch to tower at the hold-short, by handoff + `CT`; snapshotted); (2) `FrequencyState` keyed per position (`Enqueue`/`ExpectReadback`/`AcknowledgeControllerResponse`/`AwaitingReadbackFrom` per frequency; replaces `HasLeftStudentFrequency`); (3) pilot and AI-controller transmissions tagged with their frequency so the client plays only the frequencies the human monitors (student: own position; instructor: selectable monitor set) and AI controller instructions occupy airtime on their own frequency with the per-position voice; (4) inter-position coordination stays off-frequency (plan 02's coordination bus); (5) **radio discipline (steer 2026-09-01)**: an exchange owns its frequency until it completes or times out — controller instruction → pilot A readback, and pilot A request → controller response → pilot A readback — so pilot B's proactive calls and follow-ups queue behind it and an AI controller never keys up while a readback is pending (today's `FrequencyState` only holds other pilots for the 8 s "awaiting controller response" window after a request; the instruction→readback leg and the readback leg after a response are unprotected). Land as its own milestone between CA1 and CA2 — see [controller-ai/README.md](./controller-ai/README.md) decisions

## Previous focus (session 2026-08-31 — live-traffic requests, in order)
- [x] **Live-session traffic filters** — implemented + aviation-reviewed 2026-08-31 (awaiting commit); see yaat-server [live-traffic-swim/10-live-session-filters.md](../../../yaat-server/docs/plans/live-traffic-swim/10-live-session-filters.md)
- [x] Aviation-review follow-ups from the filters review — all four closed 2026-08-31 (awaiting commit): (a) no-plan discrete-code shadows now best-guess VFR (`CreateShadow` via `RulesOf`; the filter stays stricter); (b) **no change needed** — `ConflictAlertDetector.IsPairEligible` already excludes shadow↔shadow pairs categorically, so distant-volume CA noise is structurally impossible (shadow↔simulated pairs only exist where the instructor put simulated traffic); (c) SFDPS `initialFlightRules` is only ever IFR/VFR on the wire, **but TAIS `flightRules="E"` is the enroute-hosted IFR plan, not VFR — fixed** (was mislabeling ~105k reports/hr); (d) TAIS plan `<type>` (A/P/E) decides what its single `<airport>` means — now parsed, `P` (proposed departure) maps to Departure instead of Destination
- [x] **Real-world track ownership + controller roster** — implemented 2026-08-31 (awaiting commit): shadows tracked by their real controlling positions (TAIS cps gated by ocr, SFDPS controlling/handoff), pending handoffs display, feed yields silently to TRACK/ASSUME, roster synthesizes "Real World" positions; see yaat-server [live-traffic-swim/11-feed-ownership.md](../../../yaat-server/docs/plans/live-traffic-swim/11-feed-ownership.md)
- [x] Carry TAIS scratchpads onto shadows (question 2026-08-31): `LiveTrack.ScratchPad1/2` → `LiveTrafficSample.Scratchpad1/2` → `AircraftStarsState.Scratchpad1/2` on every sample, under the same feed-yield gate as ownership (empty = feed cleared the pad)
- [x] SWIM plan open questions — 60-min capture processed 2026-08-31; evidence recorded in yaat-server `08-remaining-work.md` §5 (positionTime = radar time; ASDE-X id disagreements are real at ~2 %; 48 % of flights publish from >1 centre; census rarities all zero). Probe promoted to yaat-server `tools/swim-plan-questions.py` (awaiting commit)
- [x] Display-field audit vs vatsim-server-rs / swim-runner-rs (2026-08-31): ranked candidates in yaat-server `08-remaining-work.md` §6; the four top picks wired 2026-08-31 — TAIS `lld` → `GlobalLeaderDirection` (owning-TRACON anti-flap), TAIS `assignedBeaconCode` → FP beacon, cps coarse-vs-specific precedence fix, SFDPS `pointout` → ERAM point-outs; the rest stay unchecked in §6 by value ranking
- [x] **Cross-ARTCC scenario grants (visiting mentors)** — implemented 2026-09-01 (awaiting commit): the server now enforces `permitted = token home ARTCC ∪ operator grants` on `CreateRoom` + `GetScenarioJsonById` (it never checked the client's ARTCC before); grants live in the private yaat-server repo at `src/Yaat.Server/Data/artcc-grants.json` and ship in the image; the desktop client shows an ARTCC picker on Create Room when more than one is permitted and the room's ARTCC drives the session — see `docs/vatsim-auth.md` § ARTCC gate
- [x] Steer 2026-09-01: a visiting controller must still be able to join (or be pulled into) another ARTCC's room regardless of grants — verified the ARTCC gate never touches `JoinRoom`/`PullRpo`; pinned by `ArtccGrantHubTests.JoinRoom_OtherArtccRoom_NoGrant_Succeeds` and stated in `docs/vatsim-auth.md` + `USER_GUIDE.md`
- [ ] Design the ASDE-X identity-override rule (override identity, never the track key) now that the wire evidence exists — yaat-server `08-remaining-work.md` §5
- [x] **Steer 2026-09-03: `tools/test-all.ps1` runs the two repo suites in parallel** — done. Measured back-to-back on one machine: 117 s → 81 s, 31 % faster. Short of the theoretical 48 s because the suites contend for CPU (yaat ~66 s → ~73 s, yaat-server ~48 s → ~72 s when concurrent). Builds stay sequential on purpose — `yaat.slnx` already contains the server project, so two MSBuild processes would race the same `obj/` trees. Output is captured per job and printed whole in a fixed order; the failure path was verified with a deliberate failing test (reports `FAILED: Test yaat-server`, exit 1) rather than assumed. The shared `%LOCALAPPDATA%/yaat` cache is deliberate: it is only written when navdata/CIFP is stale (once per AIRAC cycle) and the loser of that race falls back to bundled TestData, whereas a per-job `YAAT_APPDATA_DIR` would mean a cold cache on every run
- [ ] Test-suite speed follow-ups — see [test-suite-speed.md](./test-suite-speed.md) (runner migration, profile, and the first fixes shipped 2026-08-26; pathfinder visited-set, CIFP airport index, and test warm-ups in progress)
- [ ] Pilot AI for solo training — see [pilot-ai-self-training/README.md](./pilot-ai-self-training/README.md) (M10.x readbacks/TTS shipped; M11–M12 subplans are the roadmap)

## Next up
- [ ] **Tick-path unification — TOP PRIORITY (steer 2026-09-02; controller AI waits).** The design was re-derived clean-room this session; the decisions are ADRs [0001](../adr/0001-state-equivalence-is-the-tick-contract.md)–[0006](../adr/0006-decompose-simulationengine-before-adding-to-it.md) and the vocabulary is [`CONTEXT.md`](../../CONTEXT.md). It replaces the deleted `tick-loop-unification.md`, which scoped to post-physics only and carried three factual claims that did not hold. Ordered steps, each green on its own, incremental to main:
  - [x] 1. Oracle first — shipped 2026-09-02. `src/Yaat.Sim/Simulation/Oracle/` (`SnapshotTreeDiff`, `DivergencePath`, `DivergenceAccumulator`, `TickOracleBaseline`, `OracleExemptions`) + yaat-server `tests/Yaat.Server.Tests/Oracle/TickOracleTests.cs` + the accepted-divergence baselines at yaat-server `docs/tick-oracle/`. See [tick-loop.md](../tick-loop.md) § the tick oracle. Started as live vs test alone, S2-OAK-4 seed 7, 600 s, ~11 s; four run kinds over three fixtures after 1b below. Regenerate with `YAAT_ORACLE_REBASELINE=1`
    - **14 accepted divergences, every one attributed in the baseline's `Note` fields.** `ProcessAutoAccept` (7 paths: `Track.Owner.*`, `HandoffAccepted`, `Eram.RecentHandoff*`, `Stars.SharedState` — the mutation check confirms exactly this set), `ProcessDelayedHandoffs` (`HandoffPeer`, `HandoffInitiatedAt`), pre-physics `ApplyAutoTrackConditions` (`DelayedHandoffQueue[*]`), `AdvanceLiveSecond`'s position-history sampling, and the `TickConflictAlerts` `InternalAirports` argument (the engine path had no approach-corridor suppression, so it *over*-alerted — **retired in 3c-0**, leaving 13). Step 5 retires the rest
    - **Finding: physics is identical *without weather*.** Zero position/altitude/speed/heading divergence over 600 s — the whole gap between the paths is ATC/track state, which is what ADR 0003 relocates. 1b's weather fixture is the exception and the only thing in the oracle that moves an aircraft
    - [x] **1b — the missing legs and the two blind-spot fixtures**, shipped 2026-09-02 (yaat `4b11ac6`/`c581a72f`/`e7be3fdc`/`fb9289bd`, yaat-server `72810531`/`dc48160a`/`06dbeee6`/`8bebf36a`). Four run kinds now, three fixtures, nine baselines at `<fixture>.<pair>.baseline.json`; 25 s in the default suite. This is what unblocks 3b
      - **`live-vs-replay`** — `ReplayOneSecond` runs the *same* `TickPrePhysics/TickPhysics x4/TickPostPhysics` sequence as `TickOneSecond`, so it differs only in the replay guards. Predicted the same 14 paths as live-vs-test before generating; got exactly that. The leg is therefore the instrument for 3b rather than new information today. Rooms are loaded through the server's own `LoadScenarioSeededAsync` and armed afterwards via the new `SimulationEngine.ArmReplay`, so the scenario load is not a variable
      - **`live-vs-reconstruct`** — `RecordingManager.ReconstructViaServerTick`, the only genuinely different code path (it runs the live `TickProcessor` post-physics list). Predicted one path, position history; got exactly that. Near-disjoint from the other two, the shape ADR 0003 predicts
      - **`weather` fixture** — a sub-threshold wind ramp separates all three advance semantics at once (live gated by `HasMeaningfulChange`, replay/reconstruct ungated, engine absent). **This is the oracle's first physics divergence**: wind, track, heading, position, then altitude and speed. Every weather-less sweep still shows physics identical on all four run kinds
      - **`autodelete` fixture** — has to use the queued-`DEL` branch: `ScenarioLoader.cs:423` exempts every scenario-declared aircraft, so the `Parked`/`OnLanding`/departed-overflight gates are unreachable without generated traffic. The sweep asserts an aircraft actually disappeared, since a fixture that stops reaching its gate produces a spotless baseline that reads as agreement
      - All 70 new entries attributed. Three paths were not predicted (`Declination`, `Phases.ConfigSet`, the beacon-code pool) and are second-order consequences of ones that were
      - Also landed: `Describe`'s shrink branch now reads as a regression and withholds the re-baseline command (the trap recorded below), pinned by `TickOracleBaselineTests`; and regeneration is byte-stable, so anything `git status` reports on a baseline is a real change
    - **Blind spots that remain** (recorded in the test's doc comment): coordination timers and solo-training evaluation reach no fixture; **the replay leg is weaker than it looks** — no fixture declares a runtime generator, so the generator stand-down guard is never reached (asserted, not assumed), and recording suppression is invisible in principle because the action log is not simulation state and is not in `ScenarioSnapshotDto`
    - **Blind spot found 2026-09-04 — action-application fidelity is outside the oracle's reach by construction.** Both oracle legs replay the same action log through the same applier, so an action the live server applied through a *different arm* than replay does never shows up as a divergence path. Found from the S2-OAK-5 bundle: the CRC `AS 3O DA R22` echo was recorded beside its `RecordedAmendFlightPlan`; live routed it through the flight-plan arm, reconstruction pushed it through `CommandDispatcher`, and a `VfrHoldPhase` cancelled on it — the bundle's snapshots showed the R22 flying off while the live log kept it hovering. Fixed by `RecordedCommandKind.FlightPlan` (both replay paths skip; dispatcher refuses at its public entries). Pinned by replay-vs-live-log tests on the real recording (`FlightPlanCommandReconstructionKeepsPhaseTests`, `FlightPlanCommandReplayKeepsPhaseTests`). **The class is step 3d below** — the 2026-09-05 audit found it was one of eight, and `bug_bundle.py history` tags CRC echoes (`CRC`) so a bundle triage sees them
    - [ ] Not yet compared: the recording-corpus sweep, and ADR 0004's post-refactor hash + step-trace mode — both step 5's shape rather than step 1's
  - [x] 2. `SimulationEngine` decomposition — shipped 2026-09-02 (`f5bee1d0` split, `6c17062a` extraction). Closes the backlog item below. Ownership map: [tick-loop.md](../tick-loop.md) § the engine's partial files
    - **Partial split by cluster**, 5,546 lines / 175 members in one class → 12 files, largest `SimulationEngine.Tick.cs` at 1,114 (deliberate — it is the file steps 3 and 4 add to). Pure move, verified two ways: an exact-coverage assertion in the generator (no line assigned twice, none unassigned) and a code-line multiset comparison afterwards (4,883 = 4,883)
    - **`Simulation/Replay/ReplayDriver.cs`** (463 L, internal) now owns the recorded-action cursors; the engine's replay methods are one-line delegators, so none of the ~500 test call sites moved. Three copies of the cursor-seek loop (fast-forward, replay load, snapshot restore) collapsed into one `SeekTo`
    - **What stayed on the engine, each because something outside replay depends on it**: the two replay mode flags (four non-replay members read them; replaced by `RunProfile` in 3b), `_replayTrackApplier` (`DispatchAiCommand` uses it live), `SnapshotIntervalSeconds` (yaat-server's `RecordingManager` reads it in production), `TickTimings`, and `AccumulateTiming` — the last a genuine miscategorisation caught by the compiler, since `TickPhysics` calls it five times and it is therefore on every run kind's path
    - Both commits green on the full cross-repo suite, unchanged at 11,304 + 2,088
  - [ ] 3. Spine over the whole sim-second, host + run profile split, `host` rename across both repos (ADRs 0001, 0005). Three commits, rename first; the spine holds every simulation-affecting step in **current live order**, server-owned bodies included, as typed named slots a host fills (step 4 then converts slot bodies without touching ordering)
    - [x] **3a — the `host` rename**, shipped 2026-09-02. 43 code files + 7 `git mv`s + 21 docs across both repos; mapping table and the three deliberately-kept uses recorded in [ADR 0005](../adr/0005-host-and-run-profile.md). Oracle baseline byte-identical; suites unchanged at 11,304 + 2,088
    - [x] **3b — the run profile**, shipped 2026-09-03 in two commits. **A** (yaat `b08547f6`, yaat-server `4987d5c0`): the generator stand-down bug fixed first under TDD — `Generators.cs:83-84` stood the generators down in replay only if the log carried a recorded spawn (a compat shim for pre-`RecordedAircraftSpawn` recordings, `15c55592`), so a spawn-less recording replayed with live generators inventing traffic; now a replay never runs them. A census of the 128 fixture recordings found 2 with generators and no recorded spawns, neither with generated traffic in its snapshots, and none desynced. **B**: `Simulation/RunProfile.cs` — `RunKind {Live, Replay, Test, Soak}` + `RunProfile` with the three allowances (`RecordsActions`, `RunsGenerators`, `RunsControllerAi`, false only for Replay); `SimulationEngine.RunProfile` (default Test) + `EnterReplay()` scope the driver runs every step under; both engine flags deleted. **Not pure-yaat after all:** yaat-server reads neither engine flag but *writes* `IsPlaybackMode` at 8 sites and Yaat.Sim read it at 4 of the 5 guards, so the server now sets the profile through one seam (`RoomEngine.EnterPlayback`/`LeavePlayback`; `TakeControl` = leave + truncation; `TrainingRoom.HostRunKind` seeds every engine it creates — Live, Soak for `HeadlessRoom`, Replay for the temp reconstruction room). `IsPlaybackMode` stays as server room-lifecycle state. Settled guard count: 5 sites, 8 flag reads (4 + 4). Pinned by `RunProfileTests` (per-entry-point scope, recorders) and yaat-server `RunProfileTransitionTests` (every transition), because the oracle cannot see any allowance. **No host interface was minted** — every member one would carry (`internalAirports`, the action applier, the AI service, the terminal emitter) is a spine slot, so it arrives with 3c-0/3c
    - [x] **3c-0 — ordering alignment first**, shipped 2026-09-03 in six commits (yaat `8c8d5f8e` `b8df9889` `34bb6b6f` `6e136f7d` `1458d0f5` `447ad8e3`, yaat-server `9fedb859`). **There are three ordering moves, not four.** ADR 0002's "fourth" was a double-count of the two membership items, inherited from the deleted design note that listed them under both axes with a parenthetical saying so; the eight adjudications are now enumerated in [ADR 0002](../adr/0002-ordering-defers-to-live-semantics-defer-to-merit.md) itself, which is the adjudication record the spine's list gets written against. Three independent re-derivations agree on three (inversion count decomposes into three relocations; LIS of the 13-step permutation is 10). Landed: `internalAirports` resolved inside Yaat.Sim from `SimScenarioState.ArtccConfig` + the student facility, retiring baseline entry 14 on two legs (14→13, the other seven baselines byte-identical); PilotProactive engine-2nd→after the detectors; speech before readbacks; strip dispatches last. **The oracle could only judge the first of those four** — the three ordering moves touch terminal entries and room-owned strip state, neither in `StateSnapshotDto`, and the full suite passed byte-identically across moves 2 and 3, so `PostPhysicsDrainOrderTests` was added as the only instrument that can see them (both orderings mutation-checked). Suite 11,330 + 2,097. **Still unpinned by anything: `TickPilotProactive`'s position relative to the detectors** — no proactive rule reads what they write, so it has no behavioural observable until 3c's step trace
    - [x] **3c — the spine**, shipped 2026-09-04 byte-identical to all nine baselines. `src/Yaat.Sim/Simulation/Spine/` (`SpineOrder` — the three lists in live order, `StepId`, `SpineStep`, `IHostSteps` + `IHostConsumers` = `ISimulationHost`, `StepTrace`, `BareHost`) + `SimulationEngine.Spine.cs` (segment entry points `BeginSecond` / `OpenSecond` / `RunPrePhysics` / `RunPhysicsSubTick` / `RunPostPhysics` / `RunEndOfSecond`, composed by `RunSecond`; *segment*, not phase — CONTEXT.md). Four hosts: `BareHost`, `ReplayHost` (Yaat.Sim), `LiveRoomHost`, `ReconstructionHost` (yaat-server, over an abstract `RoomHost` that maps every slot onto one `TickProcessor` body). `TickProcessor`'s two lists, its timing closure and `RoomEngine.BeginSecond`/`RunSecondPhysics`/`AdvanceOneSecond` are gone; the harness ticks through `AdvanceLiveSecond`; `RoomTickLoopService.PhysicsSubTickRate` deleted (one constant); `TickTimings` is one opt-in sink on the engine. **The step trace** (`(StepId, subTick)` per second, FNV-1a digest, counts; on by default) is asserted equal to live's on every oracle leg every second, hard, never baselined; `SpineTraceTests` pins the literal sequence (finally pinning `TickPilotProactive` after the detectors) and proves `ReplayOneSubTick` ×4 = `ReplayOneSecond` by digest *and* snapshot. Mutation-checked four ways: a swapped pair (trace test red), an emptied live slot (oracle: six auto-accept paths GONE), an extra sub-tick on one host (oracle trace assertion at t=1), a new `IHostSteps` member (every host fails to compile). Two things the critique changed: `BeginSecond` and `OpenSecond` are two calls because the sub-tick step keeps its own clock, and the METAR issuer now anchors to `TickProcessor.Clock` so the harness's full live second cannot flake on wall-clock minute 53. Not the plan's original check: deleting a step from the shared list is invisible to the oracle by construction (every run kind loses it together) — only the literal-sequence test sees that
    - **Prerequisite: met 2026-09-02** by step 1b above — the oracle now drives the replay leg, the reconstruct leg, and weather. The guard-count discrepancy it inherited (ADR 0005 "eight", 3b "five sites, two reads") was settled with 3b: five sites, eight flag reads, recorded in ADR 0005
    - **The trap this sequencing exists for:** a live-side regression makes a divergence *disappear*, which the oracle reports under `Removed` — and `TickOracleBaseline.Describe` prints "divergence path(s) GONE — if that was the intent, re-baseline to bank it". A regression presents as a congratulation with a suggested fix. 3c commits must be baseline-neutral in `Added` **and** `Removed`; re-baselining is not an available response there
  - [ ] **3d — the action router (steer 2026-09-05: "incorporate the replay-fidelity work into the tick plan").** The action-path twin of 3c: a recorded controller action is routed by *three* independent parse-and-decide chains — live `RoomEngine.SendCommandAsync` (~37 arms), Sim replay `SimulationEngine.ReplayCommand`, server reconstruction `RecordingManager.ReplayCommand` — and the tick oracle cannot see any disagreement between them by construction. The 2026-09-05 audit found the 2026-09-04 flight-plan skip arm was one of a class: **`SQALL`/`SNALL`/`SSALL`, `TAXIALL`, `TDLSOPS`, `ASDXALERTS`, `ADD` silently no-op on every replay and bundle export** (global commands recorded with an empty callsign, dropped by the appliers' aircraft-exists guard; `ADD` also draws `World.Rng` live and records no spawn, desyncing the RNG from then on); **server reconstruction ignores `ReactionDelaySeconds`** (every bundle snapshot reacts at the issue second) and never rebuilds `SoloTrainingEvaluator` after `RewindAsync` resets it; two track tables (`TrackCommandHandler` live/reconstruct vs `TrackEngine.Dispatch` Sim replay) and two identity resolvers with opposite AI-branch precedence; typed `FP`/`DA`/`RMK`/`APT` record no command text while the CRC path records both; `CFR` is never recorded. Plus live mutations that never reach the log at all: CRC-console CONS/DECON (`RecordAndDispatch` refuses `ConsolidateCommand`), per-TCP shared STARS state, clearances/hold annotations, Request Strip (both entry points), ASDE-X/SAID temp data. Design and evidence: the session plan file → promoted to [ADR 0007](../adr/0007-one-action-router.md) at 3d-6. Decisions: scope = router + the unrecorded CRC mutations (attendance / wall-clock timestamps / strip+TDLS snapshot coverage stay in step 4 below); full post-dispatch mirror on every run kind (read-backs are sim state, the host decides broadcast); rejected commands are recorded and flagged. Sub-commits, each green cross-repo, predicted-then-generated baselines (3c's discipline):
    - [x] **3d-0 — instrument first**, done 2026-09-05 (committed). The `actions` oracle fixture (`OracleFixture.Script` + `ReactionDelaySeconds`; S2-OAK-4, seed 7, 120 s; script: `AS 3O`, a reaction-delayed `HPPL`, `SQ 1234` → `SQALL`, `SSALL` … `SNALL`, a parked `ADD` → `TAXIALL`, `ASDXALERTS`, `ACCEPTALL` twice across `AS 4U`, `HO 4U`, `FP`, `CON`/`DECON`, `GHOST`, one refused verb; every live step asserts its expected verdict because a refused command is not recorded) + `actions.live-vs-{test,replay,reconstruct}.baseline.json`, 25 / 21 / 69 entries, every one attributed. **Predicted and got**: the added aircraft absent and the shared RNG one draw behind on every leg from t=20; `Transponder.Code/Mode` after the squawk globals on every leg (only once the script gave them state to change — the first cut left no trace because every aircraft already squawked its assigned code, which is itself the lesson: a fixture must reach state, not just the arm); `Track.Owner.Subset` from t=42 on every leg (replay/test: `ACCEPTALL` no-op; reconstruct: the `AS 4U` position switch is dropped, so `ACCEPTALL` resolves as the student — the "possible" `SetActivePosition` gap is confirmed); `DeferredDispatches` + the whole orbit cascade on reconstruct only (reaction delay ignored, 4 s early); `FlightPlan.*` on test only (the bare dispatcher refuses `FP`; replay/reconstruct apply the amendment). **Not predicted**: `HasMadeInitialContact` at t=5 on test/replay — second-order, `RegisterControllerContact` reads `Track.Owner`, already diverged at t=1 by the step-4 auto-track family (the same 12 paths as `s2oak4`); `Cid` of the ghost on reconstruct — second-order of the RNG; `Server.ConsolidationOverrides` on all three legs — live `HandleConsolidate` and replay `Engine.ReplayConsolidate` are two arms (3d-3/3d-4 to reconcile). **Not scriptable here**: `TDLSOPS` (no ZOA facility enables DCL op configs), typed `RPOSLOC` (no live arm; reposition is CRC-only), the CRC `AS {tcp} DA` echo — all stay on the completeness sweep and the real-recording pins. Also landed: `RecordedCommandClassifier.ClassifyParsed`; `ActionRoutingCompletenessTests` (Yaat.Sim.Tests) banks 13 unrouted types exactly (`ADD`, `ASDXALERTS`, `BM`, `CFR`, `PAUSE`/`UNPAUSE`/`SIMRATE`, `TAXIALL` — a refusal arm — and the five TDLS verbs) plus three phase-gated-only arms (`AssignRunway`, `GO`, `TaxiAuto`); `RecordingCorpusRoutingCensusTests` over all 129 fixtures → `TestData/recording-routing-census.json` (16,555 commands): `ADD` in 5 fixtures (14 commands — `a67670e50d58`, `follow-straightin-sequencing`, `issue283`, `issue284`, `issue409`), reaction delays in 3 (`c000c2b0afc8` ×7, `issue193-mia-cto-lineup` ×4, `mia-spawn-replay-cross12` ×13 — the real-recording pins for 3d-4), 6 solo rooms, legacy `PAUSE`/`UNPAUSE`/`SIMRATE` records in 17 older fixtures (transport, correctly inert), and two `SEPM` separator moves in `swa1089-spawn-taxi-spin` whose pre-qualifier bay tokens the current grammar rejects — run `Yaat.RecordingUpgrader` on that bundle. **ERAM sweep (Class B, B7)**: `QT` initiate / `/OK` steal write `Track.Owner` directly; `QH F` freeze, `QQ`/`QR` altitudes, `QS` heading/speed/free text and `LF` CRR labels write `Eram.*` directly — none recorded; only `QX` and `AM` route through `DispatchCrc`/`AmendFlightPlan`. The reconstruction reaction-delay test on a real recording is 3d-4's TDD test (the `DeferredDispatches` baseline entry is its pin until then)
    - [x] **3d-1 — classifier exhaustive + `ActionScope`**, done 2026-09-05 (committed). `Simulation/Actions/ActionScope.cs` (`Global` / `Callsign` / `Aircraft` / `Position` — what the router resolves *before* an arm runs; a property of the kind, not of the recorded callsign). `RecordedCommandClassifier`: `Classification(Kind, Scope, Parsed)`; ten new kinds (`TaxiAll`, `TdlsOps`, `Tdls`, `AsdexEnableAllAlerts`, `AddAircraft`, `Cfr`, `GlobalCoordination`, `SetActivePosition`, `Bookmark`, `Transport`); `ScopeOf(kind)` exhaustive; the `_ => Compound` default replaced by `IsAviationCommand` (the 145 dispatcher-owned types, listed) and `_ => throw UnroutedCommandException`. `SetActivePosition` and `GlobalCoordination` are matched *before* the track / coordination family predicates that also contain them. Behaviour-neutral by construction and by measurement: both appliers gained a `GlobalCoordination` case mirroring today's `Coordination` handling and otherwise fall through exactly as before; **all twelve oracle baselines byte-identical** under `YAAT_ORACLE_REBASELINE=1`. The arm table itself waits for the router (3d-3): a table of bodies nothing calls is dead code. `ActionRoutingCompletenessTests` now asserts exhaustiveness (every subtype classifies, every kind has a scope) and the forward parity `IsAviationCommand ⇒ dispatcher arm` (the reverse is not a drift signal — `DEL`, `SAY` and the flight-plan verbs have kinds of their own *and* a dispatcher arm); the banked unrouted list is gone because there is no default to fall into. Census regenerated: `Unparsed` (text both parsers reject) replaces `GlobalNoOps`, and the 16,555 commands now show their real kinds (7,706 aviation, 5,185 strip, 2,055 track, 990 delete, 215 flight-plan, 209 ghost, 113 transport, 31 `AS`, 21 TDLS, 14 `ADD`). Five fixtures carry text the current grammar rejects — replayed as a chain and dropped: `SEQ 2 N342T` (`921b8c537a44`, retired verb), the two `SEPM` moves (`swa1089-spawn-taxi-spin`, pre-qualifier bay tokens), `CTO 28R` ×2 (`s1-oak-taxi-deadlock`), and four routings through `TOLLPLAZA` (`66fd6538542e`, `a67670e50d58` — a custom fix the parser no longer resolves). None is a 3d defect; each is a candidate for `Yaat.RecordingUpgrader` or a test whose recorded premise has quietly changed. **Finding for 3d-3**: typed `APT` (`ChangeDestinationCommand`) is in the aviation list — live routes it through the flight-plan arm (records an amendment), replay re-dispatches it through the dispatcher's `ChangeDestination` arm, so a hold cancels on replay exactly as the DA echo did — **reversed at 3d-3**: the live bare-`APT` arm runs `TryChangeDestination` (the STAR / approach / route clear) and records only the amendment, so an amendment-only replay is the bug, not the fix; see the 3d-3 line
    - [x] **3d-2 — one identity**, done 2026-09-05 (committed). `TrackResolver.ResolveTcpToOwner(scenario, code)` absorbs the server chain (student TCP → scenario ATC → facility TCP → ERAM → **STARS interfacility handoff code, the leg Sim lacked** → ERAM-to-STARS prefix) reading `scenario.ArtccConfig`; the optional `artccConfig` parameters on `TrackResolver` / `TrackEngine` are gone. `TrackResolver.ResolveIdentity` is the one resolver (AS override → AI connection → selection → student, per `5ee2f4a0`); `TrackCommandHandler.ResolveEffectiveIdentity` / instance `ResolveTcpToOwner` / `FindTcpByCode` deleted. One map: `Simulation/Actions/PositionSelections` — **room-owned on the server** (`TrainingRoom.PositionSelections`, handed to every engine in `PopulateRoom`, so an RPO's `AS` still survives scenario reloads), engine-owned for Sim hosts; written only by `SimulationEngine.SelectPosition` (live `AS`, Sim replay, reconstruction, CRC sync); **snapshotted** in `ServerSnapshotDto.PositionSelections` and replaced on restore. Server reconstruction now applies a recorded bare `AS` and the position-scoped `ACCEPTALL`/`HOALL` (moved above the aircraft-exists guard), and both rewinds empty the room's map before reconstructing, so **a rewind restores the selections as of its target** (pinned by `PositionSelectionsRoomTests`; a selection made after the target no longer survives it). **Predicted and got**: `actions.live-vs-test` +1 (`Server.PositionSelections.ORACLE-RPO` from t=2 — the test leg's bare `SendCommand` refuses `AS`; retires in 3d-3); `actions.live-vs-reconstruct` −10 (the whole t=42 family); the other ten baselines byte-identical. **Correction to the 3d-0 attribution**: the map was not the cause — with the recorded `AS 4U` applied, reconstruction still dropped `ACCEPTALL` because the position-scoped global command sits below the aircraft-exists guard with an empty callsign (the `ActionScope.Position` case 3d-1 named); Sim replay / test still lack an `ACCEPTALL` body until 3d-3. Follow-ups: `SyncCrcPositionToRpo` writes the map without a record (Class B — record it as the connection's `AS` in 3d-5, else a from-scratch reconstruction loses a CRC-synced selection); `TrackCommandHandler` still resolves consolidation TCPs through `ArtccConfigService` (3d-5)
    - [x] **3d-3 — `ActionRouter` + `IActionHost`; Sim replay onto it.** Four green sub-commits, one predicted retirement each (the 3c discipline). Decisions taken 2026-09-05 (recorded in the session plan): `IActionHost` is its own interface, implemented by `BareHost`/`ReplayHost`, until the server hosts implement it at 3d-4 and it folds into `ISimulationHost` — no dead server bodies meanwhile; every routed command records its text accepted or not, from the Sim entry points now (`RecordedCommand.Accepted`; the live chain still records successes only until 3d-5); CRC attendance stays a host query for the consolidate arm (C1 debt named, not hidden); no solo oracle fixture (nothing the post-dispatch mirror changes is snapshotted — the Sim tests and the solo corpus are the instrument). **Bare typed `APT` stays `Compound`**, reversing the 3d-1 suggestion: live's arm runs `TryChangeDestination` (STAR / approach / route clear) and records only the amendment, so an amendment-only replay *loses* the clear; the fix is text-only recording through the aviation arm at 3d-5 (`command-pipeline.md` Pitfalls)
      - [x] **3d-3a — router + hosts + post-dispatch over today's Sim bodies**, done 2026-09-05 (committed: yaat `61545f95`, yaat-server `e71ffb08`). `Simulation/Actions/` (`ActionRouter`, `ArmTable`/`ActionArm`/`ArmContext`, `ActionArms`, `IActionHost`, `ActionInput`/`BakedDraws`, `ActionOutcome`/`ActionTrace`, `ActionRefusals`, `ReactionDelayPolicy`); `SimulationEngine.Actions`; `SendCommand` (under `LocalConnectionId`), `DispatchAiCommand` and the replay driver all route through it; `ReplayCommand`, `ApplyReplayPostDispatch`, `DispatchLiveCommand`, `TryDeferCommandForReaction`, `ReplayTrackApplier` deleted; `ApplyPostDispatch` on every run kind (read-backs, "unable" and the frequency gates are replayed sim state; the host decides broadcast); three optional record fields (`SpawnedAircraft`, `IssuedAtUtc`, `Accepted`); `CompoundPolicy.TrySplitSpecialCompound` shared with the server; one `SquawkAll` body (the server's two copies deleted) and one `TimerCommandApplier` (the live wrapper adds broadcasts); server `HandleStandardCmd` on `ReactionDelayPolicy` + `DeferForReaction`. The rows whose bodies have not crossed (ACCEPTALL/HOALL, GHOST, RPOS, TAXIALL, CONS/DECON, ADD) refuse with the host-only message until 3d-3b/c/d. **Predicted and got**: `actions.live-vs-test` −4 (`Transponder.Code`@14, `.Mode`@16, `DeferredDispatches`@38, `Server.PositionSelections.ORACLE-RPO`@2), `actions.live-vs-replay` −2 (`Transponder.Code/Mode`), nothing Added, the other ten byte-identical. One instrument fix on the way: the oracle's test leg now issues through `Actions.Issue` under the script's connection id — the bare `SendCommand` selected under `local` and would have banked a key mismatch instead of the retirement. Corpus triage (ADR 0004): `ChainAbortAndCompletionTests.TrackDispatchFailure_DiscardsRemainder` pinned a chain the live server never dispatches whole (a compound containing a track verb is split into units on live and, now, on the router) — re-pointed at the preset path, the one path that hands a track verb its chain-mates; `AiSinkRoutingParityTests` ×2 pinned "a refused AI verb records nothing" — the engine now records it as rejected, so the parity compares accepted records only until 3d-5. No solo fixture moved; no recording deleted. Suites 11,385 + 2,102
      - [x] **3d-3b — ADD + TAXIALL Sim bodies**, done 2026-09-05 (committed: yaat `57930780`, yaat-server `f67f85ea`). `SimulationEngine.AddAircraft(args, recorded)` *derives* the spawn on every run kind — `SpawnParser` → `AircraftGenerator.Generate` against the shared `World.Rng` and `BeaconCodePool` → scratchpad rules → `World.AddAircraft` (the CID draw) — so both shared streams advance exactly as live; a recorded action's baked `SpawnedAircraft` is the authority when the derivation disagrees (the derived aircraft leaves the world and the pool, the recorded one takes its place, `replay-fidelity` warning), and a pre-feature record without one simply derives. Decision change from the plan's row ("baked wins, else draw"): using the baked snapshot *without* drawing would have left the RNG and the pool one ADD behind on replay — exactly the divergence the entry banks — while derive-then-reconcile keeps both in lockstep and still survives a divergent world. `SimulationEngine.TaxiAll` is `HandleTaxiAllCmd`'s loop over `BuildDispatchContext`; the server's `HandleSpawnAircraftAsync` (returns the spawn snapshot for the record) and `HandleTaxiAllCmd` are wrappers (room hooks + broadcasts); `ScenarioLifecycleService.GetStarsConfig` deleted; `BeaconCodePool.IsAssigned` for the tests; `AddAndTaxiAllArmTests` ×5. **Predicted vs got**: NEW1's half of `Aircraft[*]` retired as predicted (the entry stays for GHOST1; onset 20→70 on test and replay). `Rng.S0..S3` did **not** retire: the onset moved 20→70 because GHOST1's CID is drawn from `World.Rng` live (`SimulationWorld.AddAircraft`) and the ghost has no Sim arm yet — the paths retire with 3d-3c, not here. Nothing Added, nothing Removed, reconstruct and the other ten byte-identical. Finding → Backlog: `TAXIALL {rwy}` is adjacent-only since #393 (below). No corpus fixture moved; no recording deleted. Suites 11,389 + 2,102
      - [x] **3d-3c — track family into `TrackEngine`**, done 2026-09-05 (committed: yaat `f4153007`, yaat-server `c2f5d9c8`). `TrackEngine.DispatchGlobal` (ACCEPTALL/HOALL), `TrackEngine.AcknowledgeConflictAlert` (CAACK on the engine's `ConflictAlerts`, dispatched by the `Track` arm before `Dispatch`), and `TrackEngine.Ghost.cs` (`CreateGhostTrack` with the 0.1 nm/ghost stagger, `RepositionToLocation`/`RepositionMove`, `ParkedDataBlockId`); arms `GlobalTrack`, `GhostTrack` (a created phantom goes to `OnAircraftSpawned`), `Reposition`; `ActionRefusals.NoActivePosition`. The server's `TrackCommandHandler.HandleGlobalTrackCommand`/`HandleGhostTrack`/`HandleReposition*`/`HandleAcknowledgeConflictAlert` are wrappers (its `ParkedDataBlockId` forwards to the Sim's); reconstruction and the live chain unchanged. `TrackFamilyArmTests` ×7; mutation (all four rows back to the refusal + CAACK branch removed) → 7/7 red. **Predicted and got**: test and replay each −6 — `Aircraft[*]`@70, `Rng.S0..S3`@70, `Track.Owner.Subset`@42; reconstruct byte-identical (`Cid`@70 stays). **Not predicted**: +3 on test and replay at t=42 — `Aircraft[*].Eram.RecentHandoffPreviousOwner.Callsign`/`.SectorId` and `Aircraft[*].Stars.SharedState.<tcp>` — second-order of the step-4 auto-track family: the ACCEPTALL now applies here and records the aircraft's *previous* owner, which already differs at t=6 (`Track.Owner.Callsign`); before, this leg left those fields null so only the parent paths showed. Attributed; retire with the step-4 family. Corpus: none of the 53 ghost-carrying fixtures moved; no recording deleted. Suites 11,397 + 2,102
      - [x] **3d-3d — CONS/DECON one body**, done 2026-09-05 (committed: yaat `678fe084`, yaat-server `31fe3f77`). `SimulationEngine.Consolidate(con, isAttended)` / `Deconsolidate(decon)` (`SimulationEngine.Consolidation.cs`) are the one body `RoomEngine.HandleConsolidate`/`HandleDeconsolidate` and the reconstruction copies `ReplayConsolidate`/`ReplayDeconsolidate` used to be (the copies deleted); `TransferTracksForConsolidation` moved with them over `scenario.ArtccConfig.GetConsolidatedDescendants` and the new `IActionHost.IsPositionAttended` query (false on Bare/Replay — the C1 attendance debt now named on the interface, not hidden in a server body); `OnConsolidationChanged` consumer; arms `Consolidate`/`Deconsolidate` replace the last two `NotAvailable` rows, so the arm table has no refusal row left. The server's `HandleConsolidationCmd` is one wrapper (terminal echoes + `StarsConsolidation` rebroadcast) and reconstruction calls the body with `RoomEngine.IsPositionAttended`. `ConsolidationArmTests` ×5 against the real NCT hierarchy (4U → 4Q/4R) with a host stub answering attendance. **Predicted and got**: test and replay each −1 — `Server.ConsolidationOverrides.<4U>`@60; nothing Added (the script's `CON 3O 4U` is basic, so no transfer and no attendance question). **Wrong for reconstruct**: its entry stays. The cause was never the body — `RecordingManager.ReplayCommand`'s aircraft-exists guard sits above its `Consolidate` case and the record carries an empty callsign, so the copy was never reached (the same class as its `SQALL`/`SNALL` gap); the Note is rewritten to the guard and the path retires at 3d-4 with `ReplayCommand`. The other ten baselines byte-identical. Suites 11,402 + 2,102; mutation (attendance answer dropped inside the body) → the attended-descendant test red
    - [x] **3d-4 — server reconstruction onto it**, done 2026-09-05; `RoomHost : IActionHost` (`ISimulationHost : … , IActionHost`); `RecordingManager.ReplayCommand`/`ApplyRecordedActionCore` deleted; the three recorded-action pumps collapse onto `RecordedActionPump` (the note that used to sit under step 4); `actions.live-vs-reconstruct` empties. Two sub-commits (decided 2026-09-05 after reading the reconstruction chain):
      - [x] **3d-4a — consolidation redirect into `TrackEngine`**, done 2026-09-05 (committed: yaat `650e0eb2`, yaat-server `8af41945`). Landed as planned below; `TrackEngine` gained its logger for the handoff log line the server body carried. **Predicted and got**: all twelve baselines byte-identical. `ConsolidationRedirectTests` ×4 (three red first — the no-attendance case already held), the redirect pins and `TrackDispatchParityTests` green on the wrappers; suites 11,406 + 2,102. No changelog bullet: Sim replay still attends nobody and the server's live and reconstruction paths kept their behaviour, so nothing a user sees changed. Reconstruction today runs the *server* track table (`TrackCommandHandler.HandleTrackCommand`), whose `HandleHandoff`/`HandlePointOut` redirect an unattended target to its attended consolidation owner (`TryConsolidationRedirect` over `PositionRegistry.IsPositionAttended` + `GetConsolidationOwner`) and re-point an inbound handoff the recipient re-addresses; the Sim table's `ApplyHandoff`/`ApplyPointOut` skip both ("depends on the live attendance map"). Routing reconstruction through the `Track` arm before closing that gap would lose redirected handoffs on every rewind. Move: `ConsolidationRedirect(scenario, ConsolidationState, isAttended)` in Yaat.Sim (the server body verbatim); `TrackEngine.Dispatch(parsed, ac, identity, scenario, redirect)` — a required parameter, `null` where no host answers (the two deferred/preset track dispatches, which never redirected); `ApplyHandoff(ac, scenario, identity, tcpCode, redirect)` gains the manual-redirect branch and the redirect branch with the server's messages; `ApplyPointOut(ac, scenario, tcpCode, redirect)` likewise; the `Track` arm passes `new ConsolidationRedirect(scenario, engine.ConsolidationState, host.IsPositionAttended)`; the server's `HandleHandoff`/`HandlePointOut` become wrappers and `TryConsolidationRedirect` is deleted. Tests: `ConsolidationRedirectTests` (Sim, NCT hierarchy: `HO 4Q` with 4Q→4U overridden and 4U attended lands on 4U with `HandoffRedirectedBy` = 4Q; the same with nobody attended does not redirect; the recipient's re-addressed inbound handoff; a point-out to 4Q lands on 4U); `CrcHandoffRedirectTests`/`PointoutConsolidationRedirectTests`/`HandoffAcceptTests` stay green on the wrappers. Predicted: all twelve baselines byte-identical (the oracle room attends nobody, so `HO 4U`@38 never redirected on any leg)
      - [x] **3d-4b — hosts onto the router; one pump**, done 2026-09-05 (committed: yaat `c47f670d`, yaat-server `d921f7ed`). Landed as planned below. **Predicted and got**: `actions.live-vs-reconstruct` emptied — all 59 entries (the ADD family, `Rng.S0..S3`, `Cid`, `DeferredDispatches` + the orbit cascade, `Transponder.Code/Mode`, `ConsolidationOverrides`); the other eleven byte-identical, `weather.live-vs-reconstruct` included. `RecordedActionHostRoutingTests` ×4 red first; the whole server suite green on the first run after the host rewrite (no corpus triage needed — `RewindTests`' cursor asserts, the legacy terminal echo, the two-way-comms and hover-hold reconstruction pins, `DynamicMetarRewindTests` all held); suites 11,410 + 2,102; mutation (the never-recorded gate removed from the router) → the inert-kinds test red. Also: `RecordingManager` lost three constructor dependencies (the handlers it no longer calls) and `IActionHost.ApplyBookmark` carries the issuing initials. Plan as written: `ISimulationHost : IHostSteps, IHostConsumers, IActionHost`; `RoomHost` implements the slots over the room's handlers (strips: the async handler's synchronous completion, else fire-and-forget as reconstruction does today; coordination: the sync handler; ASDE-X enable-all: `AsdexMutationApplier`; transport: `Pause`/`Resume`/`SetSimRate`; bookmarks: the `AddBookmark`/`RenameBookmark`/`DeleteBookmark` entry points; TDLS / TDLS ops: refused while replaying — TDLS state is not snapshotted, so a rewind cannot reconstruct it and a replayed `TDLSQ` would duplicate live state — else the async handler; flight plan: refused, the live chain owns it until 3d-5) and the consumers with the live tails gated on "replaying" (`IsBroadcastSuppressed || IsPlaybackMode`, the `LoadWeather` idiom): `OnAircraftSpawned` = `ChangeTracker.Remove` (+ `AfterAircraftSpawned` and the spawn broadcast only when fresh — a rewind must not reprint strips the live run already printed), `OnAircraftDeleted` = `ChangeTracker.Remove` (+ assignments, deletion and CRC broadcasts when fresh), `OnPositionSelected` = display config when fresh, `OnTimersChanged`/`OnHeldDeparturesChanged`/`OnConsolidationChanged` = the live broadcasts, `OnFlightPlanAmended` = the departure-strip reprint extracted from `RoomEngine.AmendFlightPlan`, `OnWeatherChanged` (new) = the room's weather mirrors + `BroadcastWeatherChanged`. Router: `ApplyRecorded` gains `RecordedAsdexMutation`/`RecordedSaidMutation` → new host slots `ApplyRecordedAsdexMutation`/`ApplyRecordedSaidMutation` (room state; Bare/Replay no-op), a live-traffic sample that spawns → `OnAircraftSpawned`, a removal → `OnAircraftDeleted`, a weather record → `OnWeatherChanged`; a never-recorded kind (`Bookmark`, `Transport`) is inert on `Apply` in the router itself, not by the host's refusal (a legacy `PAUSE` record must never pause a rewind). `ApplyRecordedWeatherChange` also drops `Scenario.MetarIssuer` (the server's `ConfigureMetarIssuer(…, false)` did on reconstruction; the host rebuilds it on return to live). `RecordedActionPump(actions)` in `Simulation/Replay/` (cursor, pre-tick set, `SeekTo`/`ApplyPreTick`/`ApplyThrough`) replaces `ReplayCursors` + `ApplyRecordedAircraftSpawnsBeforeTick`; `ReplayHost`, `ReconstructionHost` and tape playback (`LiveRoomHost`, mirroring `scenario.PlaybackCursor` and keeping the legacy terminal echo in its applier) drive it; `RecordingManager.ApplyRecordedAction(action, host)` stays as the `_applyingRecordedAction` wrapper over `Actions.ApplyRecorded` (the flag retires at 3d-5); `ApplyRecordedActionCore`, `ReplayCommand`, `ApplySettingChange`, `ApplyRecordedAircraftSpawn`, `NormalizeSyntheticAircraftSpawn`, `ReplayDeleteQueued`, `ApplyPreTickPlaybackActions`, `ApplyPlaybackActions` deleted (the `RoomEngine.ApplyPlaybackActions` test seam drives the live host's pump). Reconstruction's aviation arm now runs the full `ApplyPostDispatch` (read-backs, "unable", evaluator) instead of its contact + request-resolution subset — the 3d-3a decision extended to the last run kind. Predicted: `actions.live-vs-reconstruct` empties (all 59: the ADD family, `Rng.S0..S3`, `Cid`, `DeferredDispatches` + the orbit cascade, `Transponder.Code/Mode`, `ConsolidationOverrides`); the other eleven byte-identical — `weather.live-vs-reconstruct` staying empty is the check on the weather consumer
    - [ ] **3d-5 — live chain onto it + the Class B records.** Two sub-commits.
      - [x] **3d-5a — the live chain onto the router**, done 2026-09-05 (awaiting commit). `RoomEngine.SendCommandAsync` is room policy (assignments, the `**` override) + the terminal echo around one `IssueLive` → `Actions.Issue(input, LiveRoomHost)`; the 30-arm chain, `HandleStandardCmd`, `DispatchSpecialCompoundAsync` and every `Handle*Cmd` wrapper are deleted; the CRC entry points (`RecordAndDispatch`, `RecordAndDispatchStrip`, `RecordAndDispatchFlightPlan`) issue through the same router with `TrackResolver.AsPrefixCode`; `TrackCommandHandler` (the second track table) and `TrackDispatchParityTests` are deleted — its statics were forwards to `TrackEngine` / `TrackResolver`, and the test harness keeps a `TrackCommandSeam` over the leaves for tests that act on a resolved aircraft directly. Bodies that crossed into Sim: the flight-plan verbs (`FlightPlanNormalization` + `FlightPlanEcho` moved from the server; the arm files through `AmendFlightPlan`, records the amendment and tags `CreatedByOwner` with the filing identity — from a record only the tag; the `ApplyFlightPlanCommand` slot, `AuditOnlyWhenRecorded` and the server `FlightPlanCommandHandler` deleted), the TRACK tails (scratchpad rules + `OnTrackAcquired`, INHCA's conflict drop, a ghost's DROP cleanup, ASDE-X TERM → `OnAsdexTrackTerminated`), the aviation arm's landing-clearance layout cache and APT → `OnFlightPlanAmended`, `SpawnNow`'s held-on-spawn mark + terminal lines, and the SHOW query as its own never-recorded kind (`ShowQueued`; `SayOrShow` → `Say`; `OnQueuedCommandsShown`). Bare typed `APT` is the aviation arm, recorded as text, and `ChangeDestination` is phase-transparent (the live arm had bypassed the gate; a parked aircraft's plan stays editable). The create-FP canonical text carries its rules (`VP …`, `FP … OTP/NNN`) so the router re-derives what the CRC keyboard entered. The CRC DA/VP path issues a `GHOST` at the click position for an unknown callsign and a `DROP` if the plan is refused, both recorded — the unsupported block a replay used to lose. `RoomHost` consumers carry the fresh tails (`AnnounceAircraftSpawned/Deleted`, `HideLiveTraffic`, display config, CRC coordination / STARS-delete broadcasts, the ephemeral SHOW listing) gated on `Replaying`; `OnAircraftDeleted(callsign, lastState)`, `OnPositionSelected(…, tcpCode)`, `OnLiveTrafficHidden`, `OnTrackAcquired`, `OnGhostOverlayRemoved`, `OnAsdexTrackTerminated`, `OnQueuedCommandsShown` added. Strip / TDLS handlers are synchronous (their broadcasts fire-and-forget), so the slots return the real verdict — `CompleteOrDetach` deleted. `_applyingRecordedAction` and `RecordingManager.ApplyRecordedAction` retired (the hosts call `Actions.ApplyRecorded`; `Record` gates on `IsBroadcastSuppressed` alone); a command issued during tape playback takes control before it routes. Rejections recorded on every entry point (`AiSinkRoutingParityTests` compares every record). **Predicted and got**: `actions.live-vs-test` −3 (`FlightPlan.AltitudeCruiseFeet/Destination/RevisionNumber` @50 — the bare leg files the FP now); nothing Added; the other eleven byte-identical. Suites 11,418 + 2,105. **Behaviour changes to know**: a typed `FP`/`VP`/`DA` tags the filing position as the plan's creator (the STARS auto-track acquires it on the assigned squawk), as a CRC-entered one always did; any command typed during playback takes control, a refused one included; `SIMRATE` echoes as typed with a response line; the terminal's FP echo is one line; `INHCA` / `CAACK` typed live no longer need an active position (one identity guard, `TrackEngine.RequiresIdentity`, on every run kind); a `DROP` of a pure ghost is a plain CRC delete (it never coasts), as before.
      - [ ] **3d-5b — the Class B records**: `HandleCrcCoordination` (CRC coordination through the handler, unrecorded) → `RecordAndDispatch` text; `SyncCrcPositionToRpo` recorded as the connection's `AS`; `RecordedConsolidationChange`, `RecordedStarsSharedStateChange`, `RecordedClearanceChange`/`RecordedHoldAnnotationChange`, `RecordedStripRequest`, `RecordedSurfaceTempDataChange`, and for the ERAM keyboard entries `QT`/`QH`/`QQ`/`QR`/`QS`/`LF` either `AS {tcp} <verb>` text where a canonical verb exists or one `RecordedEramEntry(Callsign, Field, Value)`
    - [ ] **3d-6 — docs**: ADR 0007, CONTEXT.md (action router, arm, baked draw), `command-pipeline.md` (one table; `:211` "records include rejects" was false until now), `snapshots-and-replay.md`, `tick-loop.md` § Hosts, `bug_bundle.py history`
  - [ ] 4. Relocate tick-reachable ATC logic into `Yaat.Sim`; TDLS and coast as sim core + wire projection; attendance as a recorded input; server session-persistence DTOs collapse into the Sim snapshot (ADR 0003)
    - [ ] Attendance as a recorded input (`PositionRegistry.IsPositionAttended`/`IsTcpControlledByCrc` gate `ProcessAutoAccept`, `ProcessPointoutAutoAck` and the delayed-handoff consolidation redirect — `TickProcessor.cs:1086-1094, 1225, 1310`; a temp reconstruction room has zero attendance, a same-room rewind sees today's)
    - [ ] Sim-time, not `DateTime.UtcNow`, for `TdlsItemRecord.CreatedUtc/SentUtc/WilcoUtc/ExpiresUtc` (`TdlsCommandHandler.cs:80,139,142,190`, `TickProcessor.cs:282,293,340`) and the strip PDT/ETA text (`StripMutations.cs:878-903`) — every rewound PDC and strip shows the reconstruction's clock today
    - [ ] `FlightStripState` / `TdlsState` into `ScenarioSnapshotDto`, retiring `RoomStateSnapshotMapper`'s duplicate (today rewind never restores either; only session persistence does)
    - [ ] Shrink `IActionHost` as each host-slot body (strips, TDLS, coordination, ASDE-X/SAID, bookmarks) crosses — the action path's step-4 debt, same shape as `IHostSteps`
  - [ ] 5. Retire the accepted-divergence entries one at a time; switch the oracle's default to hash + step trace (ADR 0004)
    - [x] **`AutoDelete` and `SoloTrainingEvaluation` run on every path** (the two membership adjudications, 2026-09-04): both are sim steps whose results the host consumes (`OnAutoDeleted`, `OnSoloTrainingEvents`); `IHostSteps` lost two members. Predicted and got: `autodelete.live-vs-test` and `live-vs-replay` each lose exactly `Aircraft[*]` and `Server.BeaconCodePool.AssignedCodes.1`, the other seven byte-identical; the fixture now asserts every room deleted. Corpus triage, five tests, all over-broad: four asserted on an arrival still parked after the second it parked — a state only the bare path ever kept, since live removes it that second — and now send `NODEL` first, the instructor's own way to keep it (`OakGroundE2ETests`, `OakPostLandingReversalsTests` ×3); `Issue10OnHoldShortDeleteTests` observed the hold-short phase after a whole second and now steps by segment. `SweepPendingAutoDeletes` and `AiTestFixture`'s hand-rolled sweep deleted. The solo evaluator keeps its own record of what it scored and mutates no aircraft from `Evaluate` (its one aircraft write is on the command path), so a replayed solo session now rebuilds that record; no fixture is a solo room, so the oracle sees nothing here — still a blind spot
    - [x] **Position-history sampling is a sim step** (2026-09-04): `SimulationEngine.SamplePositionHistory` at the head of `EndOfSecond`; the two constants moved to `AircraftState`; `IHostSteps` lost the member and `RoomEngine` its consts. Predicted `Aircraft[*].PositionHistory` gone from all nine baselines; got six retirements and, on the three weather baselines, a *shift* to `PositionHistory[*].Lat/.Lon` — the lists now agree in shape and the sampled positions differ because the wind cascade already moves the aircraft, so those three retire with the weather semantic. `autodelete.live-vs-reconstruct` is the first baseline with an empty `Entries` list
    - [x] **One weather-advance semantic** (the third semantic adjudication, 2026-09-04): `SimulationEngine.AdvanceWeatherTimeline` re-collapses the timeline every second, ungated, on every run kind; the host's `OnWeatherAdvanced` mirrors `Room.Weather`; `HasMeaningfulChange` and its tests deleted. The gate only ever existed to rate-limit a broadcast that had moved to the METAR issuer. Aviation-reviewed: 7110.65 §2-6-3/-4/-5, §3-1-8, §3-5-1, §3-5-3, §13-1-14 and AIM 7-1 / 4-3 are silent on how a simulated wind field is discretised (every FAA quantisation is a *reporting* one and already lives in `MetarIssuer` / `SpeciCriteria`); continuous is the faithful semantic; runway-in-use (30° / 5 kt hysteresis) and SPECI (45° / 10 kt) cannot chatter on sub-degree ticks. One caveat acted on: `WeatherProfile` carries per-instance METAR caches, so `WeatherTimeline.BuildProfile` now hands back the same instance for the same static period (inside a transition a new profile per second is inherent). Predicted: the wind cascade and its second-order entries retire on all three weather baselines, `weather.live-vs-reconstruct` empties — got exactly that
    - **Residual after the three retirements (2026-09-04):** every remaining baseline entry is ATC/track state the host still owns — `ProcessAutoAccept` (7 paths), `ProcessDelayedHandoffs` (2), the autotrack `DelayedHandoffQueue` (3) — on `s2oak4` and `weather` live-vs-test / live-vs-replay; both live-vs-reconstruct baselines and all three autodelete ones are empty. Those retire only as step 4 relocates each body (the `IHostSteps` header lists them as step-4 debt). The physics side of the oracle is now identical on every run kind, weather included
    - Follow-ups surfaced by 3c, not in scope: `MetarIssuer` anchors routine :53 issuance to the load-time clock (now `TickProcessor.Clock`, so the harness is deterministic; sim-time anchoring proper is step-4 material alongside the ASDE-X coast); the three pre-tick recorded-action loops (`ReplayHost`, `RecordingManager.ApplyPreTickPlaybackActions`, `ReconstructionHost`) collapse onto one sim helper in 3d-4; the sub-tick replay step runs pre-physics at a fractional clock — `SpineTraceTests` proves ×4 = ×1 by snapshot on a bare fixture with no delayed spawn, so a spawn due at `t` is still unpinned there; `ReplayRangeWithVerification` / `SnapshotDriftReport` have no callers; `TickTimings` is opt-in (the hash oracle decides whether trace and timing share one switch)
  - Known live behaviour changes to land as named decisions: ASDE-X/SAID coast moves off wall-clock to sim-time (ERAM coast already is); the three post-physics ordering moves; `DrainAllApproachScores` consumed on every path
  - Aviation-review findings (2026-09-02, over ADRs 0002/0003; citations verified against the local 7110.65):
    - [ ] **Auto-accept must be suppressed while a track shows CST.** §5-4-5.5 (transferring controller) and §5-4-6.f.3 (receiving controller), both IFR and VFR, require *verbal* coordination when CST/FAIL/NONE/IF/NT/TRK is displayed. `ProcessAutoAccept` has no coast check today, so a coasting track is silently auto-accepted. Only possible once ADR 0003 brings coast state across the boundary in a form the auto-accept path can read — land the two together
    - [ ] **Pointout auto-ack inverts §5-4-7.a.1.a**: "If the receiving controller takes no action, revert to verbal procedures." Non-response must not become approval. Withdraw the pending point-out on timeout and advise the initiator to coordinate verbally — that also clears the flashing indicator the current timer exists to solve. (En route; §5-4-7.a.1.b makes automated approval terminal-only under a facility directive or LOA)
    - [ ] **Fix the false ERAM-coast citation** at `CrcVisibilityTracker.cs:531` — there is no §5-13-8.3; §5-13-8 governs a *controller manually initiating* a coast track and prescribes no automatic reacquisition. The §5-13-7 citations elsewhere are correct and stay (it says a coast track may not be used for separation, which is exactly what they claim)
    - [ ] Coordination expiry warning fires at 120 s of a 180 s window, so the "about to expire" state is on for two-thirds of its life — move to the last 30-60 s (`TickProcessor.cs:1301`)
    - [ ] Product call: raise the 3 s solo auto-accept floor (`TickProcessor.cs:1342`) to at least the 5 s default so a student sees a handoff sit pending — a training-value judgement, no citation. `HandoffUnacceptedRule` derives its horizon from the same constant
    - [ ] Separate issue, pre-existing: `ApproachEvaluator.ComputeSeparation` (`:164`) measures the trailer's establishment position against the leader's *current* position, including aircraft that have already landed and taxied in — not an in-trail separation at a common instant. Consuming scores on every path spreads the number to more surfaces
  - [x] Latent bug found during the review, fixed independently 2026-09-02 (`5ee2f4a0`): `_replayTrackApplier` was documented replay-only but `DispatchAiCommand` uses it on the live path, so a recorded `AS` carrying an AI connection id displaced the live AI's identity. `ResolveEffectiveIdentity` now resolves the AI branch before the shared selected-position map; pinned by `ReplayTrackApplierIdentityIsolationTests`
- [ ] **Controller AI v1 — on hold behind the tick work** (was Phase 1 of [12-milestone-v1-scope.md](./controller-ai/12-milestone-v1-scope.md); DET-03 is subsumed by the ADRs above)
- [x] ~~**Cut a release** — run `/prepare-release` (steer 2026-09-02)~~ — done: `v0.12.25-beta` is tagged and dated 2026/09/02, with only the ADR / oracle / decomposition commits after it
- [x] ~~Adopt GSD as part of the workflow~~ — trialled and **reverted** 2026-09-02. `gsd-core` uninstalled; `.planning/` deleted; `CLAUDE.md` moved back to the repo root. The durable output was salvaged into `docs/`: [`controller-ai/11-radio-model.md`](./controller-ai/11-radio-model.md), [`controller-ai/12-milestone-v1-scope.md`](./controller-ai/12-milestone-v1-scope.md), the Phase-1 decisions since superseded by the tick-path ADRs above, and the codebase-audit findings as Backlog items below.
- [ ] Live-session assume UX (ZOA Discord 2026-08-31): bulk-assume modes, snapshot-then-assume-all, auto-assume on first command + restore-to-feed command, snapshot-as-scenario-authoring — see yaat-server [live-traffic-swim/09-live-sessions.md](../../../yaat-server/docs/plans/live-traffic-swim/09-live-sessions.md) §3
- [x] `tools/make-oak-northfield-scenario.py` — gitignored 2026-08-31 (user chose local-only over committing)
- [ ] Live-traffic follow-ups from the 2026-08-31 aviation review (receipt-recency model shipped): (a) gate/qualify `RunwaySafetyAdvisor.WarnIfTrafficOnFinal` / `WarnIfLiveTrafficOnRunway` on a coasting shadow (7110.65 5-13-7); (b) deliberate validation pass over the three behaviors the fix un-deadened (shadow-vs-simulated conflict alerts, `GroundAcceleration`, assume coast note); (c) surface per-track observation age to the instructor (5-1-1 judgment)
- [ ] Live session: selecting ZOA > NCT > OAK_APP picks SJC as the primary airport instead of OAK — debug the position-to-primary-airport resolution (reported 2026-08-31)
- [ ] Live traffic via SWIM (#150) — see [open-issues/150-live-traffic-swim.md](./open-issues/150-live-traffic-swim.md) → yaat-server `docs/plans/live-traffic-swim/08-remaining-work.md` (all slices shipped 2026-08-28; **blocked** on FAA ADX access to the LADD list before the feed can go live on YAAT1, then a first-month soak)
- [ ] RPO limited-access mode + VATUSA ARTCC auto-fill — see [rpo-limited-access-and-vatusa-artcc.md](./rpo-limited-access-and-vatusa-artcc.md) (17 open)
- [ ] CRC protocol support gaps — see [crc-protocol-support.md](./crc-protocol-support.md) (13 open of 174; status table of the CRC hub protocol)
- [ ] **Controller AI + soak-testing harness** — see [controller-ai/README.md](./controller-ai/README.md); the active v1 slice is [12-milestone-v1-scope.md](./controller-ai/12-milestone-v1-scope.md) (H0/CA0/CA1/K1 shipped; Phase 1 = tick-loop unification, designed and next)
- [ ] vTDLS emulation v1 — see [vtdls-emulation.md](./vtdls-emulation.md) (pre-work landed; remaining PDC flow)

## Backlog
- [ ] **`CAACK` inside a chain or preset silently no-ops** (finding 2026-09-05, tick 3d-3c review): `TrackEngine.Dispatch` returns null for `AcknowledgeConflictAlertCommand` (its state is the engine's `ConflictAlerts`, which the per-aircraft method never sees); the router's `Track` arm (the one table since 3d-5a) dispatches it first, but `SimulationEngine.DeferredCommands` (`TryDispatchImmediateTrackPreset`, `ProcessTriggeredTrackBlocks`) call `Dispatch` directly and only branch on `Success == false`, so `FH 090, CAACK` acknowledges nothing and says nothing. Pre-existing; no test covers it. Fix: hand `Dispatch` the conflict-alert set.
- [ ] **`TAXIALL {rwy}` is adjacent-only** (finding 2026-09-05, tick 3d-3b): the command builds an empty-path `TaxiCommand`, and a bare `TAXI {rwy}` has been adjacent-only since #393, so every aircraft parked away from the runway fails with "not at runway … use TAXIAUTO" and only the count in the response says so. Live and Sim agree (the 3d-3b port is faithful); the fix is a behaviour decision — route each aircraft as `TAXIAUTO {rwy}` does (`SimulationEngine.TaxiAll`, one line) — and needs an aviation-review pass on whether an instructor's "everyone taxi to 28R" should imply a route.
- [ ] **Review the docs structure — user-facing vs internal dev docs** (steer 2026-09-02): the repo root carries USER_GUIDE.md, COMMANDS.md, SOLO_TRAINING.md, GETTING_STARTED.md, INSTALL.md alongside `docs/` (60+ subsystem docs, `docs/plans/`, `docs/user-guide/`). Decide the boundary and where each audience starts.
- [ ] **Regenerate `docs/scenario-validation-known-failures.md`** — its last full run is dated 2026-03-12, ~6 months stale, and substantial command-pipeline and chaining changes have shipped since (chained-command hardening in v0.12.23-beta, CTO/CTOC behaviour changes). Some of the 231 catalogued failures may now parse differently. Re-run yaat-server's `python tools/validate-all-scenarios.py`.
- [ ] **Archive two resolved plans still sitting in `open-issues/`** — the repo's own hygiene rule says finished plans get promoted or archived: [172-taxi-crossing-holdshort-and-directionality.md](./open-issues/172-taxi-crossing-holdshort-and-directionality.md) (marked implemented and verified 2026-06-03, says it "can be archived once merged") and [fillet-s-turn-connectors.md](./open-issues/fillet-s-turn-connectors.md) (titled "superseded for #236"; documents why the original approach was rejected — worth keeping, in `archive/`).
- [ ] **Audit the rest of the hub surface for client-trusted authorization** — the ARTCC entitlement fix (server refuses to open a room or fetch a catalog scenario for an ARTCC the signed-in controller is not entitled to, instead of trusting the client's ARTCC) shipped very recently. Similar client-trusting patterns may exist in other `TrainingHub.cs` methods; nothing has swept them.
- [x] ~~**`SimulationEngine.cs` decomposition — decide whether to**~~ — decided and done 2026-09-02 (ADR 0006, tick step 2 above): 12 partial files plus a `ReplayDriver` extraction. Adjacent hotspots, still undecided, for blast-radius awareness when planning parallel work: `PatternCommandHandler.cs` (3,967), `MainViewModel.cs` (3,915, 20 commits — second-most-churned), `CommandParser.cs` (3,797), `CommandDispatcher.cs` (3,657), `MainWindow.axaml.cs` (3,452), `GroundCommandHandler.cs` (3,041).
- [ ] Standalone airport GeoJSON editor — see [airport-editor.md](./airport-editor.md) (51 open; not started)
- [ ] Phraseology coverage — see [phraseology-coverage-backlog.md](./phraseology-coverage-backlog.md) + [phraseology-implementation.md](./phraseology-implementation.md) (handoff doc for the rule backlog)
- [ ] "Show nav route" overlay limitations — see [nav-route-overlay-followups.md](./nav-route-overlay-followups.md)
- [ ] BEHIND grammar extensions — see [open-issues-deferred/behind-grammar-extensions.md](./open-issues-deferred/behind-grammar-extensions.md) (18 open; deferred)
- [ ] Fillet S-turn connectors — see [open-issues/fillet-s-turn-connectors.md](./open-issues/fillet-s-turn-connectors.md) (superseded by the pathfinder fix; optional nicety)
- [ ] Bounded HOLDP (EFC model) — time/circuit-limited holds per 7110.65 §4-6-1.c; `HoldingPatternPhase.MaxCircuits` already self-completes, only the HOLDP argument + release path are missing (from the 2026-08-31 chain-hardening aviation review)
- [x] Live identity-gate divergence (inhibit/acknowledge-conflict-alert required an AS identity live, not on replay) — gone with 3d-5a (2026-09-05): one table, `TrackEngine.RequiresIdentity`, on every run kind
- [ ] FOLLOWG chain E2E — two-aircraft test proving `FOLLOWG X; CROSS <rwy>` fires the crossing at the hold-short (predicate-level pin exists in `IndefiniteHoldMarkerTests`; regime-C firing covered generically)

## Reference (shipped, kept for context)
- [x] Distance measuring tool — [distance-measuring-tool.md](./distance-measuring-tool.md)
- [x] Favorites identity model — [favorites-identity-model.md](./favorites-identity-model.md)
- [x] STARS behavioral sweep (#372) — [stars-audit.md](./stars-audit.md)
- [x] Command/tick synchronization — [command-tick-synchronization.md](./command-tick-synchronization.md)
- [x] Bug-finding expedition 2026-07 — [bug-hunt-2026-07.md](./bug-hunt-2026-07.md)
- [x] Taxi crossing / hold-short precedence (#172) — [open-issues/172-taxi-crossing-holdshort-and-directionality.md](./open-issues/172-taxi-crossing-holdshort-and-directionality.md)
- [x] Archived: [asdex-safety-logic.md](./archive/asdex-safety-logic.md), [eram-audit.md](./archive/eram-audit.md), [post-physics-ownership-refactor.md](./archive/post-physics-ownership-refactor.md), [controller-ai-solo-training.md](./archive/controller-ai-solo-training.md) (superseded by [controller-ai/](./controller-ai/README.md))

## Blockers
- None recorded.
