# M10.7 Session Report and Separation Scoring

Solo training now has a live Session Report that replaces the old approach-only report. The report is available from **Scenario > Session Report** and refreshes while the scenario is running.

## Implemented Slice

- Live student-facing scorecard with score, grade, coaching notes, active issues, event timeline, approach scores, and runway statistics.
- FAA-grounded airborne separation scoring for:
  - IFR radar separation using 7110.65 §5-5-4 / §5-5-5 and vertical minima from §4-5-1.
  - Class B VFR separation using 7110.65 §7-9-4.
  - Class C IFR/VFR separation using 7110.65 §7-8-2 / §7-8-3 and the loaded FAA AIS airspace geometry.
  - Class C outer-area IFR/VFR participation using a documented v1 training approximation: within 20 NM of the Class C primary airport and at or below the loaded Class C ceiling when airport position data exists.
  - Separation standards are resolved from current, 30-second projected, and 60-second projected aircraft positions/altitudes so projected Class B/C entry uses the airspace shelf that will apply at the scoring sample.
  - VFR/VFR traffic in Class C and other no-minima proximity cases are not scored as separation conflicts; they create Advisory / Visual traffic-advisory findings when the recipient aircraft is still in student service.
  - Visual-follow suppression when traffic has been reported in sight and a follow instruction is active.
- FAA-grounded runway separation scoring for actual runway-operation trigger points:
  - Departures starting takeoff roll behind departures or landings using 7110.65 §3-9-6(a)-(b).
  - Arrivals crossing the landing threshold behind departures or landings using 7110.65 §3-10-3(a).
  - Reciprocal/opposite-direction use of the same physical runway is scored as conservative same-runway separation until the preceding departure crosses the runway end or the preceding landing is clear.
  - Physically intersecting runway operations are scored using 7110.65 §3-9-8 for departures and §3-10-4 for arrivals, requiring the preceding aircraft to pass the intersection/flight path or clear the runway where applicable.
  - Nonintersecting converging runway operations are scored when projected flight paths intersect within the runway path or the documented v1 1 NM beyond-DER training boundary, using 7110.65 §3-9-9 for departures and §3-10-4 for arrivals.
  - SRS Category I/II/III distances from FAA ACD `Srs`, with fallback to the §3-9-6 note definitions when the database has no category.
  - Live Session Report rows show plain-language required and actual runway-separation evidence for these events.
- FAA-grounded wake scoring for actual runway/approach trigger points:
  - Departure wake intervals from 7110.65 §3-9-6(f)-(g) and intersection-departure intervals from §3-9-7(a).
  - CWT mile-based separation from 7110.65 §5-5-4(g) can satisfy the departure wake requirement when it is already tighter than the time interval.
  - Terminal same-runway and close-parallel approach spacing uses 7110.65 §5-5-4(h) Table 5-5-2.
- FAA-grounded traffic-advisory scoring for aircraft pairs already covered by the live airborne separation minima:
  - Missing advisory proof follows 7110.65 §2-1-21, with Class B mandatory-advisory coverage also citing §7-9-5 and Class C IFR/VFR service coverage also citing §7-8-2.
  - Accepted structured `RTIS <clock> <miles> <direction> <type> <altitude>` commands prove only the directed recipient-target advisory; accepted RTIS soft-fails still count because the advisory was issued.
  - RPO shorthand `RTIS <target>`, bare `RTIS`, and `RTISF` still drive visual-acquisition/control-flow convenience outside solo training, but they no longer count as solo-training advisory proof and are rejected in solo-training mode.
  - Structured `RFIS <clock> <miles>` records field-in-sight proof and runs the same field-acquisition / soft-fail observation path as the existing field-in-sight workflow; bare `RFIS`/`RFISF` remain RPO-only visual-acquisition shortcuts.
  - Coach, warning, and safety advisory events mirror the underlying projected or current separation state and clear when proof is recorded.
- FAA-grounded visual-approach field-proof scoring:
  - Accepted `CVA` without `FOLLOW` creates an Advisory / Visual warning while the aircraft lacks field-in-sight proof, citing 7110.65 §7-4-3 and AIM §5-4-23.
  - Structured `RFIS <clock> <miles>` or the normal pilot field-in-sight report clears the warning. `CVA FOLLOW` continues to rely on traffic-in-sight / follow proof.
- Student-service eligibility for advisory proof:
  - Missing advisory, safety-alert, and field-proof events are emitted only for recipient aircraft that have made initial contact, have not been transferred away with `CT`/`FCA`, and are not owned by another student position when owner data is available.
  - Pilot initial contact uses shared handoff-aware eligibility from ARTCC custom data: rules can allow contact on handoff initiated, handoff accepted, or with no handoff.
  - ARTCC custom data can match broad position-type pairs or exact callsigns under `Data/ARTCCs/{ARTCC}/InitialContactTransfers/*.json`; ZOA is seeded with APP/CTR transfer rules plus an SFO Tower no-handoff rule.
  - Target matching still uses all known aircraft because the described traffic does not need to be on the student's frequency.
- FAA-grounded safety-alert proof for current unsafe aircraft proximity:
  - `SAFAL <clock> <miles> [L|R] [C|D]` follows 7110.65 §2-1-6 and resolves the target by clock position and whole-mile distance.
  - `SAFAL` suppresses missing safety-alert scoring only for the directed recipient-target pair in a current Safety-severity unsafe-proximity state.
  - `SAFAL` does not set traffic-in-sight, does not satisfy `FOLLOW`/`CVA FOLLOW`, and creates an Advisory / Visual overuse event when issued outside a current Safety-severity state.
- Training-only terminal warnings labeled separately from controller-authentic CRC conflict alerts.
- STARS/CRC conflict alerts are intentionally not used as scoring inputs; scoring derives applicable minima from 7110.65 plus airspace/runway geometry.

## Follow-Up Scope

- Projected-flight-path crossing wake intervals, facility wake waivers, and wake-advisory phraseology proof.
