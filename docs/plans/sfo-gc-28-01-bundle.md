# SFO GC 28/01 bundle — triaged reports

One RPO session: `S1-SFO-2 _ Ground Control 28_01 (High Intensity Optional).yaat-bug-report-bundle.zip`, client 0.13.4-beta, SFO, 2152 s. It was triaged into 15 issues on 2026-09-22, one issue per root cause. Wave 1 (#448–#453) shipped 2026-09-22. Each remaining issue body carries the evidence, code references and a hypothesis. The hypotheses are unverified, and two of them (#453, #454) turned out wrong once reproduced. So every fix starts with a failing test at HEAD (`test-fix`).

**The recording.** The full 22 MB bundle is not in the repo; ask the user for it. A 400 s trim is committed as `tests/Yaat.Sim.Tests/TestData/issue453-asa826-giveway-recording.yaat-bug-report-bundle.zip`. It covers the first 400 s only, and every remaining item happens later. For a replay test, trim the full bundle around the item's time (`bug_bundle.py trim`), install it (`bug_bundle.py install --issue N`), then regenerate the routing census for the new fixture (`YAAT_ROUTING_CENSUS_REGENERATE=1` on `RecordingCorpusRoutingCensusTests`; the diff must be a pure addition). A dispatch-level test on the real SFO layout, built from a pose read out of the bundle (`bug_bundle.py snapshot --at T --callsign X`), is cheaper and was enough for #451, #452 and the #454 repro.

**The full bundle** is at `X:\Downloads\S1-SFO-2 _ Ground Control 28_01 (High Intensity Optional).yaat-bug-report-bundle.zip` on the maintainer's machine (user 2026-09-22).

**Decisions (user 2026-09-22):**
- #454/#461, a TAXI whose named route doesn't reach its destination (shipped 2026-09-23): every named taxiway is taxied for real, never swapped for a sibling. From the named lane a stand is cut to directly: across the apron to a point one fuselage out on the stand's centreline, then in on the stand heading (user-approved from the D1 renders); the sibling-lane cut is the fallback. After the last named taxiway the aircraft continues only across ramp taxilanes and the apron, plus an implied lead-in (the destination's own taxiway, only apron after it, ≤ 1,000 ft driven). If reaching the destination needs any other movement-area taxiway, it taxis the named route and holds short of that taxiway (`HoldShortReason.RouteIncomplete`), with the note `Holding short of X: route to <dest> needs X, not in clearance`, and the pilot says "…request further taxi". A runway-entry connector that isn't a numbered variant always holds short; a numbered one (W → W1) is implied. The echo and pilot readback say the clearance as issued: added taxilanes and the implied connector are silent, other uncleared movement-area taxiways are warned. Reference: `docs/ground/pathfinder.md` (Incomplete clearances), `COMMANDS.md` (TAXI).
- #456 (user 2026-09-23): a `TAXI … $spot` given to an aircraft on the ramp is a line-up to leave the ramp: it arrives on the spot from the ramp side and stops nose-on-mark facing out along the lane toward the movement-area taxiway the lane joins (SFO `TAXI T7A $7A` → facing ~027° toward A). It never swings out toward the taxiway to turn back in. The mirror case (user 2026-09-23): `TAXI A $7A @F8` from the movement area enters the ramp through that spot — off A onto T7A at the junction, through 7A heading into the ramp, along T7A's taxilane — then turns into the gate. Shape of the outbound line-up (user 2026-09-23, on seeing it render): not a sharp loop far down the lane — a smoother turn of about 90° onto the lane close to the spot, then a short slow straight pull onto the mark once lined up. Also to render and review: `PUSH $7A`, `PUSH $7` and `PUSH $7B` from the nearby gates — the RPOs' main way to get an aircraft off a gate onto a spot. Ruled out (2026-09-22, rendered at `.tmp/456-options` in the taxi-456 worktree): a lead-in point on the lane's extended line (nose 13 ft from A's centreline, ending facing 212° into the ramp) and the plain approach leg (ends facing 227°).
- Movement vs non-movement for the #454 rule is settled (user 2026-09-23, shipped): `MovementAreaClassification`'s five ordered rules, documented in `docs/ground/pathfinder.md` (ramp-lane cut). Sidecar overrides are only for one-offs checked against the real airport.

**Lessons (#454/#461):**
- The `SegmentExpander` SimLog category is nearly silent; the expander's decisions go to `ExplicitPathOptions.DiagnosticLog`. Pass that callback in a repro test before concluding which path dropped a taxiway.
- The "no node on the route to D1 is worth cutting to the stand from" log line comes from `RampLaneReposition.TryPlanResolvedRouteCut` (a route that already reached the stand), not from the destination-cut planner. It says nothing about why the named lane was skipped.

Take the waves top to bottom. Wave 3 measures before it designs. Wave 4 is the features.

## Wave 3: ground motion and push geometry

**How the push-planner rethink is designed (user 2026-09-23):** whenever it is unclear how a push should be designed, render the candidate options as LayoutInspector tick-playback HTML, one run per option (TickRecorder JSON → `--ticks --html`, with the involved taxiways highlighted and the key nodes annotated), and send them to the user for feedback before anything is built. The user picks or corrects the shape from the renders; no push geometry is decided from reasoning alone.

- [ ] **SFO push demo video** (user 2026-09-23; the per-form push readbacks it shows off shipped): drive a real YAAT session through the yaat-client-driver MCP server with the `video-capture` skill — an empty SFO scenario, `ADD` aircraft at gates, the worked `PUSH` cases — with the command overlaid on the ground view as a checklist/chapter list (an ffmpeg `drawtext`/`overlay` post-pass). Run it with solo training's pilot TTS on so each push's spoken readback is in the video's audio (user 2026-09-23: a bit more of a showoff) — capture the audio with the video. **Recorded after Wave 4 lands** so it shows the new forms too (user 2026-09-24). Cases: spot pushes (`PUSH $7A`/`$7`/`$7B` from nearby gates), a taxiway push with a facing, and multi-leg `PUSHM` including a route drawn in the ground view. Audio: the video-capture skill's per-process loopback (`Record-Window.ps1 -Audio`, added 2026-09-24; check first that the pilot TTS plays from the client process itself, since only that process tree is recorded), which records only the client's own audio whatever the output device; fallback the "What U Hear (Sound BlasterX G6)" loopback via dshow, with the G6 as Windows' default output. ffmpeg 9.0.2 is on PATH (WinGet), with ddagrab, `h264_nvenc` and ffprobe.
  - **Recording kit (2026-09-24 session; all in the main checkout's untracked `.tmp/`):** yaat-server from source on `:5130`; client launched by the driver's `launch_yaat` with appdata `.tmp/client-driver/appdata` (its `preferences.json` already sets `lastScenarioFolder` = `.tmp/demo`, solo mode, pilot voice, speech bubbles, no automatic call-ups; the Piper voice pack is installed there). Room: File > Connect > "Local", then Join/Create a **ZOA** room, Scenario > Load Scenario > Local Files > "Demo | SFO pushback" (`.tmp/demo/sfo-push-demo.json`, empty SFO). Ground View: HS and ADW off. Window: `pwsh .tmp/move-window.ps1 -ProcessId <pid> -X 1580 -Y 120 -Width 1936 -Height 1119` (a 1920×1080 client area, no activation); zoom with `pwsh .tmp/wheel.ps1 -ProcessId <pid> -X <screenX> -Y <screenY> -Notches <n>` (posts `WM_MOUSEWHEEL`; ~9 notches in from the fit view over F8 frames F-gates to spots 7A/7/7B). Commands: `set_text` on `CommandInput`, then a separate `send_keys {ENTER}` (a one-shot `text{ENTER}` loses the text). Record: `cmd /c .tmp/demo/rec.cmd <out.mp4> <seconds>` — WgcCapture by `--hwnd` plus the client's process audio (edit the hwnd and pid in the file for a new client); wait for each aircraft's call-up before recording. Post: crop the ground view out of the 1922×1112 capture with `crop=1166:656:300:128`, scale to 1080p, keep the command + readback at 1× and speed the tow up; caption each clip with its command (`drawtext`); loudness-normalise (the radio-effect voice is ~−48 dB RMS). Multi-leg tows take >4 min at 1× — record those at `SIMRATE 4`.
  - **Clips taken** (`.tmp/demo/`, 1922×1112 + AAC): `clip1.mp4` F8 CRJ7 `PUSH $7A` (60 s; readback "push to spot seven alpha"), `clip2.mp4` F7 E175 `PUSH $7` (60 s), `clip3.mp4` F11 A320 `PUSH A FACE E` (75 s). Rejected: F9 `PUSH $7B` ("Unable, cannot line up on spot 7B from here"). Still to take once `PUSHM` is fixed: F8 `PUSHM $7A $7B` and a drawn route (right-click the aircraft → Push route…, click points, right-click the last marker → Send route).
