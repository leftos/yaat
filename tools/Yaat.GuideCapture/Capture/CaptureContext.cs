namespace Yaat.GuideCapture.Capture;

// Per-run shared state. Phase A is a placeholder; Phase B adds the in-process
// yaat-server reference (server URL, lifecycle handle) and Phase C adds scene
// action helpers (load scenario, select aircraft, open flyout).
internal sealed class CaptureContext { }
